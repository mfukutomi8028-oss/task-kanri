# Ver.150 release notes

- 完了状態のタスクに固定が残らないよう、自動固定解除を追加しました。既に完了済みで固定が残っているタスクもVer.150読み込み後に整理します。
- 「前提タスク」とは別に、完了順序へ影響しない「関連タスク」を追加しました。同一案件・類似対応などを相互リンクし、タスク詳細から直接移動できます。
- 関連タスク欄から「フォローアップ作成」を追加しました。新しいタスクを元タスクの情報で事前入力し、保存成功後に元タスクと自動で関連付けます。
- 個人用の「確認リマインダー」を追加しました。期限とは別に再確認日時と短いメモを設定でき、当日・期限超過分は今日ビューに表示します。
- リマインダーは現在のユーザーごとに保存し、タスク完了後は不要なリマインダーを自動解除します。ブラウザ通知は既に許可済みの場合だけ利用し、新たな通知許可要求は行いません。
- 新機能は既存の `rooms/{room}/workflowV148` 補助領域を拡張して保存し、タスク本体の既存データ構造とVer.149以前の前提タスク・保存ビューとの互換性を維持します。
