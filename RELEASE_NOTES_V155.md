# Ver.155 リリースノート

## ユーザー新規登録の保存エラー修正
- ユーザー追加時に「同じ操作を保存中です。」と表示される問題を修正しました。
- 原因は、ユーザー追加処理が `setUsers()` 内の自動保存と、その直後の `saveUserSettings()` によって同じメタ情報を二重保存していたことです。
- Ver.155 ではユーザー追加フォームを専用の安全な保存処理へ切り替え、1回の操作につき1回だけ共同データへ保存します。
- 保存中は追加ボタンを無効化し、連続クリックによる二重登録も防止します。
- Firebase のトランザクションで最新のユーザー一覧を基準に追加するため、別端末から同時にユーザーが追加された場合も既存ユーザーを上書きしません。
- 保存後は共同データを再取得して、ユーザー名と色設定が実際に反映されたことを確認します。
- 共同編集を利用しないローカル運用の場合も、同じ二重保存エラーが出ないよう対応しています。

## 互換性
- ユーザー情報の保存先 `rooms/{room}/meta`、ローカル保存キーは変更していません。
- 既存ユーザー、色設定、タスク、スケジュール等のデータ移行は不要です。
