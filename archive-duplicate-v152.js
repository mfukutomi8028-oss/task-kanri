// Ver.152 archives + non-destructive duplicate merge.
(function installArchiveDuplicateV152(){
  const W=window.WorkBoardWorkflowV152;if(!W)return;
  const AUTO_ARCHIVE_DAYS=90,DAY=86400000;let scheduled=false,drawer=null,nav=null,badge=null,search='';
  const esc=v=>String(v||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'})[c]);
  function detailId(detail){const key=detail?.querySelector?.('[data-action="delete"]')?.dataset?.operationKey||'';return key.startsWith('task-delete:')?key.slice(12):''}
  function hiddenIds(){return new Set([...Object.keys(W.v152?.archives||{}),...Object.keys(W.v152?.duplicates||{})])}
  function openTask(id){const target=String(id||'');if(!target)return;closeDrawer();let node=document.querySelector(`[data-task-id="${CSS.escape(target)}"]`);if(node){node.click();return}document.querySelector('.nav-item[data-layout="tasks"]')?.click();document.querySelector('[data-task-layout="list"]')?.click();document.getElementById('resetFilters')?.click();const task=W.taskMap().get(target),q=document.getElementById('searchInput');if(task&&q){q.value=task.title||'';q.dispatchEvent(new Event('input',{bubbles:true}))}setTimeout(()=>document.querySelector(`[data-task-id="${CSS.escape(target)}"]`)?.click(),140)}
  function ensureNav(){if(nav?.isConnected)return;const filters=[...document.querySelectorAll('.nav-group')].find(g=>g.querySelector('[data-filter="done"]'));if(!filters)return;nav=document.createElement('button');nav.type='button';nav.className='nav-item workflow-archive-nav-v152';nav.innerHTML='<span class="nav-icon workflow-archive-icon-v152" aria-hidden="true">▣</span><span>アーカイブ</span><span class="workflow-archive-badge-v152" hidden>0</span>';badge=nav.querySelector('.workflow-archive-badge-v152');filters.appendChild(nav);nav.addEventListener('click',openDrawer)}
  function ensureDrawer(){if(drawer?.isConnected)return;drawer=document.createElement('div');drawer.className='workflow-archive-shell-v152';drawer.hidden=true;drawer.innerHTML=`<div class="workflow-drawer-backdrop-v152" data-close-archive-v152></div><aside class="workflow-drawer-v152 workflow-archive-drawer-v152" role="dialog" aria-modal="true" aria-label="アーカイブ"><header><div><small>ARCHIVE</small><h3>アーカイブ</h3></div><button type="button" class="icon-button" data-close-archive-v152 aria-label="閉じる">×</button></header><div class="workflow-archive-search-v152"><input type="search" placeholder="アーカイブ済みタスクを検索" data-archive-search-v152></div><p class="workflow-archive-help-v152">完了から${AUTO_ARCHIVE_DAYS}日経過したタスクは自動で整理されます。削除ではなく、いつでも復元できます。</p><div class="workflow-archive-list-v152" data-archive-list-v152></div></aside>`;document.body.appendChild(drawer);drawer.querySelectorAll('[data-close-archive-v152]').forEach(x=>x.addEventListener('click',closeDrawer));drawer.querySelector('[data-archive-search-v152]')?.addEventListener('input',e=>{search=e.target.value.trim().normalize('NFKC').toLowerCase();renderDrawer()})}
  function openDrawer(){ensureDrawer();drawer.hidden=false;document.body.classList.add('workflow-drawer-open-v152');renderDrawer()}
  function closeDrawer(){if(drawer)drawer.hidden=true;document.body.classList.remove('workflow-drawer-open-v152')}
  function renderDrawer(){ensureDrawer();const map=W.taskMap(),allIds=[...new Set([...Object.keys(W.v152?.archives||{}),...Object.keys(W.v152?.duplicates||{})])],entries=allIds.map(id=>{const dup=W.duplicateInfo?.(id),info=W.archiveInfo?.(id)||{archivedAt:Number(dup?.mergedAt||0),archivedBy:String(dup?.mergedBy||''),reason:'duplicate'};return{id,info,task:map.get(id),dup}}).filter(x=>x.task).filter(x=>{if(!search)return true;const s=`${x.task.title||''} ${x.task.description||''} ${x.task.completedMemo||''}`.normalize('NFKC').toLowerCase();return s.includes(search)}).sort((a,b)=>Number(b.info.archivedAt)-Number(a.info.archivedAt)),list=drawer.querySelector('[data-archive-list-v152]');if(!entries.length){list.innerHTML='<div class="workflow-inbox-empty-v152"><strong>アーカイブはありません</strong><span>完了した古いタスクや重複統合済みタスクがここに表示されます。</span></div>';return}list.innerHTML=entries.map(({id,info,task,dup})=>`<article class="workflow-archive-item-v152"><div><span class="workflow-archive-kind-v152 ${dup?'is-duplicate':''}">${dup?'重複統合':'アーカイブ'}</span><strong>${esc(task.title||'名称未設定')}</strong><small>${esc(task.completedAt?new Date(task.completedAt).toLocaleDateString('ja-JP'):'完了日不明')} · ${esc(info.archivedBy||'')}</small>${dup?`<em>統合先：${esc(map.get(dup.targetId)?.title||'タスク')}</em>`:''}</div><div class="workflow-archive-actions-v152">${dup?`<button type="button" class="ghost-button" data-open-canonical-v152="${esc(dup.targetId)}">統合先を見る</button>`:`<button type="button" class="ghost-button" data-restore-archive-v152="${esc(id)}">復元</button>`}</div></article>`).join('');list.querySelectorAll('[data-restore-archive-v152]').forEach(b=>b.addEventListener('click',async()=>{const r=await W.unarchiveTask(b.dataset.restoreArchiveV152);if(r?.ok){W.notify('タスクをアーカイブから復元しました。');renderAll()}}));list.querySelectorAll('[data-open-canonical-v152]').forEach(b=>b.addEventListener('click',()=>openTask(b.dataset.openCanonicalV152)))}
  function patchVisibility(){const hidden=hiddenIds();document.querySelectorAll('[data-task-id]').forEach(node=>{const id=String(node.dataset.taskId||'');if(!id)return;const hide=hidden.has(id);node.classList.toggle('workflow-task-archived-v152',hide);node.hidden=hide});document.querySelectorAll('.board-column').forEach(col=>{const count=[...col.querySelectorAll('.task-card[data-task-id]')].filter(x=>!x.hidden).length,em=col.querySelector('.column-head em');if(em&&em.textContent!==String(count))em.textContent=String(count)})}
  function dependencyUse(id){const map=W.taskMap(),own=(W.depIds?.(id)||[]).length,others=[...map.keys()].filter(other=>other!==id&&(W.depIds?.(other)||[]).includes(id));return{own,others}}
  function mergeComments(target,source){const out=[...(Array.isArray(target)?target:[])],seen=new Set(out.map(c=>String(c?.id||'')));for(const c of (Array.isArray(source)?source:[])){const id=String(c?.id||'');if(id&&seen.has(id))continue;out.push(c);if(id)seen.add(id)}return out.slice(-300)}
  function mergeChecklist(target,source){const out=[...(Array.isArray(target)?target:[])],seen=new Set(out.map(x=>String(x?.text||'').normalize('NFKC').trim()));for(const item of (Array.isArray(source)?source:[])){const key=String(item?.text||'').normalize('NFKC').trim();if(!key||seen.has(key))continue;out.push(item);seen.add(key)}return out.slice(0,120)}
  async function mergeDuplicate(sourceId,targetId){const source=String(sourceId),target=String(targetId);if(!source||!target||source===target)return;const map=W.taskMap(),s=map.get(source),t=map.get(target);if(!s||!t)return W.notify('統合対象のタスクを確認できませんでした。',true);const deps=dependencyUse(source);if(deps.own||deps.others.length){W.notify('このタスクは前提タスク関係に使われています。誤って作業順序を変えないよう、前提タスク設定を整理してから重複統合してください。',true);return}if(!confirm(`「${s.title}」を「${t.title}」へ重複として統合しますか？\n\n元タスクは完了・アーカイブされ、説明・コメント・タグ・チェックリストを統合先へ引き継ぎます。`))return;const r=await W.ensureRemote?.(),now=Date.now(),me=W.currentUser?.()||'';if(!r){W.notify('重複統合は共同編集ONで利用できます。',true);return}try{let conflict=false;const tasksRef=r.ref(r.db,`rooms/${W.ROOM_ID}/tasks`);const tx=await r.runTransaction(tasks=>{const root=tasks&&typeof tasks==='object'?{...tasks}:{};const rawS=root[source],rawT=root[target];if(!rawS||!rawT){conflict=true;return}if(Number(rawS.revision||0)!==Number(s.revision||0)||Number(rawT.revision||0)!==Number(t.revision||0)){conflict=true;return}const sourceDesc=String(rawS.description||'').trim(),targetDesc=String(rawT.description||'').trim();let description=targetDesc;if(sourceDesc&&sourceDesc!==targetDesc&&!targetDesc.includes(sourceDesc))description=`${targetDesc}${targetDesc?'\n\n':''}---\n【重複タスク「${rawS.title||''}」から統合】\n${sourceDesc}`;const tags=[...new Set([...(Array.isArray(rawT.tags)?rawT.tags:[]),...(Array.isArray(rawS.tags)?rawS.tags:[])])],history=[...(Array.isArray(rawT.history)?rawT.history:[]),{id:`merge-${now}-${Math.random().toString(36).slice(2,7)}`,author:me,text:`「${rawS.title||''}」を重複タスクとして統合しました。`,createdAt:now}].slice(-80);root[target]={...rawT,description,tags,comments:mergeComments(rawT.comments,rawS.comments),checklist:mergeChecklist(rawT.checklist,rawS.checklist),history,updatedAt:now,updatedBy:me,revision:Number(rawT.revision||0)+1};root[source]={...rawS,status:'完了',pinned:false,completedAt:Number(rawS.completedAt||0)||now,completedMemo:rawS.completedMemo||`重複として「${rawT.title||''}」へ統合`,duplicateOf:target,history:[...(Array.isArray(rawS.history)?rawS.history:[]),{id:`duplicate-${now}`,author:me,text:`「${rawT.title||''}」へ重複として統合しました。`,createdAt:now}].slice(-80),updatedAt:now,updatedBy:me,revision:Number(rawS.revision||0)+1};return root},{applyLocally:false});if(!tx.committed)throw new Error(conflict?'競合が発生しました。最新内容を確認して再試行してください。':'統合処理を完了できませんでした。');const sourceRelations=W.relationIds?.(source)||[],targetRelations=W.relationIds?.(target)||[];await W.markDuplicate(source,target);await W.archiveTask(source,'duplicate');if(sourceRelations.length){await W.writeRelations(target,[...new Set([...targetRelations,...sourceRelations].filter(x=>x!==source))]);await W.writeRelations(source,[])}await W.writeReminder?.(source,null);W.notify('重複タスクを統合しました。');document.getElementById('closeDetail')?.click();renderAll()}catch(e){console.warn('Ver.152 duplicate merge failed',e);W.notify(String(e?.message||'重複統合に失敗しました。'),true)}}
  function patchDetail(){const detail=document.getElementById('detailBody');if(!detail||detail.classList.contains('empty'))return;const id=detailId(detail),task=W.taskMap().get(id);if(!task)return;if(W.duplicateOf?.(id)||W.isArchived?.(id)){detail.querySelector('.workflow-organize-v152')?.remove();return}const map=W.taskMap(),latest=Math.max(0,...[...map.values()].map(t=>Number(t.updatedAt||0))),sig=`${id}|${task.status||''}|${task.revision||0}|${latest}|${map.size}`;let existing=detail.querySelector('.workflow-organize-v152');if(existing?.dataset.signature===sig)return;existing?.remove();const panel=detail.querySelector('.task-detail-panel-v149[data-tab-panel="details"]')||detail,candidates=[...map.values()].filter(t=>t&&String(t.id)!==id&&!W.isArchived?.(t.id)&&!W.duplicateOf?.(t.id)).sort((a,b)=>Number(b.updatedAt||0)-Number(a.updatedAt||0)),section=document.createElement('section');section.className='detail-section workflow-organize-v152';section.dataset.signature=sig;section.innerHTML=`<div class="workflow-organize-head-v152"><div><h4>整理</h4><p>重複したタスクの統合や、完了タスクのアーカイブを行います。</p></div></div>${W.isCompleted(task)?'<button type="button" class="ghost-button workflow-archive-task-v152">▣ アーカイブ</button>':''}<div class="workflow-duplicate-form-v152"><label><span>重複として統合</span><select data-duplicate-target-v152><option value="">統合先タスクを選択</option>${candidates.map(t=>`<option value="${esc(t.id)}">${esc(t.title||'名称未設定')}［${esc(t.status||'')}］</option>`).join('')}</select></label><button type="button" class="ghost-button" data-merge-duplicate-v152>重複として統合</button></div><p class="workflow-organize-note-v152">統合元は削除せずアーカイブに残すため、経緯を後から確認できます。</p>`;panel.appendChild(section);section.querySelector('.workflow-archive-task-v152')?.addEventListener('click',async()=>{const r=await W.archiveTask(id,'manual');if(r?.ok){W.notify('完了タスクをアーカイブしました。');document.getElementById('closeDetail')?.click();renderAll()}});section.querySelector('[data-merge-duplicate-v152]')?.addEventListener('click',()=>{const target=section.querySelector('[data-duplicate-target-v152]')?.value||'';if(!target)return W.notify('統合先タスクを選択してください。',true);mergeDuplicate(id,target)})}
  async function autoArchive(){const cutoff=Date.now()-AUTO_ARCHIVE_DAYS*DAY,targets=[...W.taskMap().values()].filter(t=>t&&W.isCompleted(t)&&Number(t.completedAt||0)>0&&Number(t.completedAt)<cutoff&&!W.isArchived?.(t.id)&&!W.duplicateOf?.(t.id)).slice(0,20);for(const task of targets)await W.archiveTask(task.id,'auto')}
  function renderAll(){ensureNav();ensureDrawer();patchVisibility();patchDetail();const count=Object.keys(W.v152?.archives||{}).length;if(badge){const text=String(count);if(badge.textContent!==text)badge.textContent=text;badge.hidden=count===0}if(drawer&&!drawer.hidden)renderDrawer()}
  function schedule(){if(scheduled)return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;renderAll()})}
  window.addEventListener('workflow-v152-update',schedule);const mainRoot=document.getElementById('mainContent'),detailRoot=document.getElementById('detailBody');[mainRoot,detailRoot].filter(Boolean).forEach(root=>new MutationObserver(m=>{if(m.some(x=>x.addedNodes.length||x.removedNodes.length))schedule()}).observe(root,{childList:true,subtree:true}));ensureNav();ensureDrawer();renderAll();setTimeout(autoArchive,2500);setInterval(autoArchive,6*60*60*1000);
})();