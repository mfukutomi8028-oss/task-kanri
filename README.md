# システム課タスク共有

システム担当2名向けのリアルタイムタスク共有サイトです。GitHub Pages + Firebase Realtime Database で動作します。

## 主な機能

- Firebase Realtime Databaseによる共同編集
- ルームID付きURLで複数ルーム運用
- ユーザー選択、ユーザー追加、ユーザーカラー
- タスク追加・編集・削除
- 状態：未着手 / 対応中 / 確認待ち / 保留 / 完了
- 優先度：緊急 / 高 / 中 / 低
- 分類：PC / プリンタ / ネットワーク / 電子カルテ / Web/HP / アカウント / 業者対応 / 定期作業 / その他
- 期限日・期限時刻
- 依頼元
- タグ
- チェックリスト
- コメント・申し送り
- 固定表示
- 期限超過、今日まで、自分の担当、固定のみフィルター
- ボード表示 / 一覧表示 / 自分の担当 / 完了表示
- Firebase未設定時のローカル保存フォールバック

## GitHub Pagesへのアップロード

1. このzipを解凍します。
2. 中身をGitHubリポジトリへアップロードします。
3. `config.js` にFirebase設定を貼り付けます。
4. GitHub Pagesを有効化します。
5. 公開URLへアクセスします。

## Firebase Realtime Database設定

### 1. Firebaseプロジェクト作成

Firebase Consoleで新規プロジェクトを作成します。Google Analyticsは不要ならOFFで構いません。

### 2. Webアプリ追加

プロジェクト概要からWebアプリを追加し、表示された `firebaseConfig` を `config.js` に貼り付けます。

### 3. Realtime Database作成

Realtime Databaseを作成します。場所は近い地域で構いません。最初はテストモードでも動きますが、公開前にルールを確認してください。

### 4. ルール設定

Realtime Database の「ルール」タブに `firebase-rules.json` の内容を貼り付けて公開します。

注意：このルールは、URLを知っている人が読み書きできる簡易運用向けです。院内の実名・患者情報・パスワード・IPアドレスなどの機密情報は入力しないでください。認証付きにしたい場合はFirebase Authenticationを追加してください。

## config.js の例

```js
window.firebaseConfig = {
  apiKey: "xxxx",
  authDomain: "xxxx.firebaseapp.com",
  databaseURL: "https://xxxx-default-rtdb.firebaseio.com",
  projectId: "xxxx",
  storageBucket: "xxxx.firebasestorage.app",
  messagingSenderId: "xxxx",
  appId: "xxxx"
};
```

## データ構造

```text
rooms/{roomId}/meta
rooms/{roomId}/tasks/{taskId}
```

## 共有方法

画面右上の「共有リンク」を押すと、現在のルームID付きURLをコピーできます。
同じURLを2人で開くと同じタスクを共有できます。


## v2 修正内容

- `config.js` にFirebase設定を反映しました。
- Firebase Consoleから取得したコードに含まれる `import` / `initializeApp` / `getAnalytics` は削除しました。
- このサイトでは `app.js` 側でFirebase SDKを読み込むため、`config.js` には `window.firebaseConfig = {...}` のみ記載します。
- Realtime Database用に `databaseURL` を追加しました。
- `databaseURL` は `https://task-kanri-2ad16-default-rtdb.firebaseio.com` としています。
  - Firebase ConsoleのRealtime Database画面に表示されるURLが異なる場合は、`config.js` の `databaseURL` だけ差し替えてください。


## v3 修正内容

- 初期ユーザー（福冨・森井）も削除できるようにしました。
- タイトルを「システム課タスク共有」から「タスク共有」に変更しました。
- ヒーロー右側の「共有リンク」ボタンを削除しました。
- 「＋ 新しいタスク」ボタンを検索バー左側へ移動しました。
- 左上の「管」アイコンをユーザー提供画像へ変更しました。
- 統計カードと左メニューのアイコンをユーザー提供画像へ変更しました。
- 分類管理画面を追加しました。
  - 分類の追加
  - 分類名の変更
  - 分類の削除
  - 既存タスクに使われている分類を変更/削除した場合、タスク側も反映
- 分類一覧はFirebaseの `rooms/{roomId}/meta/categories` に保存されます。


## v4 修正内容

- タイトル枠の高さを少し小さくしました。
- 共有ルーム表示をタイトル左下ではなく、タイトル枠の右下へ移動しました。
- 右側の空きスペースを活かす配置に変更しています。
- 画面幅が狭い場合は、共有ルーム表示がタイトル下へ自然に回り込むようにしています。
- `style.css?v=4` / `app.js?v=4` にしてブラウザキャッシュを回避しています。


## v5 修正内容

- タスク詳細の「完了にする」ボタンを緑の強調ボタンへ変更しました。
- 左メニューのアイコン画像を少し大きくしました。
- 左上のブランドアイコンを、ユーザー提供の3枚目アイコンへ変更しました。
- favicon / apple-touch-icon も同じ3枚目アイコンへ差し替えました。
- `style.css?v=5` / `app.js?v=5` / 各アイコン画像 `?v=5` にしてブラウザキャッシュを回避しています。


## v6 修正内容

- 一覧表示時に、ボード用の「未着手・対応中・確認待ち・保留」列が出ないように整理しました。
- 左メニューを2グループ構成に変更しました。
  - 表示：ボード / 一覧
  - 絞り込み：自分の担当 / 完了
- 「ボード表示で自分の担当」「一覧表示で自分の担当」「ボード表示で完了」「一覧表示で完了」のように、表示形式と絞り込みを組み合わせられるようにしました。
- タスク詳細に「作成日」と「更新者」を追加しました。
- 左上のタスク共有アイコンとブラウザタブアイコンを、サイトの雰囲気に合わせた新しい角丸アイコンへ変更しました。
- `style.css?v=6` / `app.js?v=6` / 各アイコン画像 `?v=6` にしてブラウザキャッシュを回避しています。


## v7 修正内容

- 状態セクションを追加・名称変更・削除できるようにしました。
  - ボード画面の右端に「＋ セクション追加」を追加
  - 左メニューの状態フィルター横に「編集」を追加
  - 状態はFirebaseの `rooms/{roomId}/meta/statuses` に保存
- 追加した状態は以下へ反映されます。
  - ボードの状態カラム
  - タスク追加/編集画面の状態プルダウン
  - 左メニューの状態フィルター
- `完了` は完了ボタンで使用するため、削除不可の固定状態にしています。
- タイムライン表示を追加しました。
  - 左メニューの「表示」グループに `タイムライン` を追加
  - 14日分の期限日ベースで表示
  - 前へ / 今日 / 次へ の移動ボタン
  - タイムライン上のタスククリックで詳細表示
  - 期限なしタスクの別枠表示
- タイムライン用の新しいアイコンを追加しました。
- `style.css?v=7` / `app.js?v=7` / 各アイコン画像 `?v=7` にしてブラウザキャッシュを回避しています。


## v8 修正内容

- 左メニューのボード・一覧・自分の担当・完了アイコンを再作成し、白いフチが見えないようにしました。
- タイムラインのアイコンを、よりシンプルで分かりやすい横軸アイコンに変更しました。
- タイムライン内のユーザー丸アイコンの文字が中央に来るようCSSを修正しました。
- 状態管理で、状態の順番を上下ボタンで入れ替えられるようにしました。
- 分類管理で、分類の順番を上下ボタンで入れ替えられるようにしました。
- 状態を新規追加した場合は、通常運用で扱いやすいように「完了」の手前へ追加されます。
- `style.css?v=8` / `app.js?v=8` / 各アイコン画像 `?v=8` にしてブラウザキャッシュを回避しています。


## v9 修正内容

- 状態セクションが6個以上になっても下段へ折り返さず、横スクロールで右側に並ぶようにしました。
- ボード表示の状態カラムをCSS Gridから横並びFlex表示に上書きしています。
- 横スクロールバーの見た目も少し調整しました。
- スマホ幅でも縦に折り返さず、横スクロールで確認できるようにしています。
- `style.css?v=9` / `app.js?v=9` / 各アイコン画像 `?v=9` にしてブラウザキャッシュを回避しています。


## v12 修正内容

- v10/v11で発生していた表示停止・ボタン無反応の原因を再調査し、v9をベースに安全に作り直しました。
- 原因はv10のドラッグ対応追加時に、関数の置換範囲が崩れ、JavaScriptの構造が壊れていたことです。
- 今回は動作していたv9から再実装しています。
- 状態管理画面で、状態をドラッグ＆ドロップで並び替えできるようにしました。
- 分類管理画面で、分類をドラッグ＆ドロップで並び替えできるようにしました。
- ユーザー管理画面で、ユーザーをドラッグ＆ドロップで並び替えできるようにしました。
- ボード画面の状態セクションも、見出し左のハンドルをドラッグして並び替えできるようにしました。
- 以前の上下ボタンで1つずつ移動するUIは非表示にしています。
- `style.css?v=12` / `app.js?v=12` / 各アイコン画像 `?v=12` にしてブラウザキャッシュを回避しています。


## v13 修正内容

- 「おすすめ順」の並びを実務向けに修正しました。
- 以前は `固定 → 優先度 → 期限` が中心でした。
- v13では以下の順番で表示します。
  1. 固定表示
  2. 期限超過
  3. 今日まで
  4. 優先度
  5. 期限が近い
  6. 更新が新しい
  7. 期限なし
- 優先度だけでなく、期限超過・今日期限を強く上に出すようにしています。
- 同条件の場合はタスク名順で安定して並ぶようにしました。
- `style.css?v=13` / `app.js?v=13` / 各アイコン画像 `?v=13` にしてブラウザキャッシュを回避しています。


## v14 修正内容

- タスク詳細パネルを、通常時は非表示にしました。
- タスクをクリックした時だけ、これまで通り右側に詳細パネルが表示されます。
- 詳細パネルを閉じる方法を追加しました。
  - 右上の `×` をクリック
  - 画面中央の何もない場所をクリック
- 詳細パネルが閉じている間は、中央のタイトル・統計・ボード・一覧・タイムライン領域が右側まで広がります。
- 詳細パネルが開いている時だけ、右側に360pxの詳細領域を確保します。
- 狭い画面では、詳細パネルが右側から重なる形で表示されます。
- `style.css?v=14` / `app.js?v=14` / 各アイコン画像 `?v=14` にしてブラウザキャッシュを回避しています。


## v15 修正内容

- タスク詳細を非表示にしてメイン領域が広がった時、新しいタスクボタンが横長になりすぎる問題を修正しました。
- `＋ 新しいタスク` ボタンは固定幅寄りにし、検索欄が広がるように調整しました。
- 通常幅では `新しいタスク 260px / 検索欄 可変 / 並び順 220px` の比率にしています。
- 画面幅が狭い場合は、これまで通り1列表示へ切り替わります。
- `style.css?v=15` / `app.js?v=15` / 各アイコン画像 `?v=15` にしてブラウザキャッシュを回避しています。


## v16 修正内容

- タイムラインに `14日 / 1か月` の切り替えを追加しました。
  - 1か月は31日分を横スクロールで表示します。
  - 前へ / 次へ は、14日表示では7日単位、1か月表示では31日単位で移動します。
- 状態セクションをユーザーごとに管理できるようにしました。
  - 状態一覧は `rooms/{roomId}/meta/statusesByUser` に保存されます。
  - 旧形式の `meta/statuses` がある場合はフォールバックとして読み込みます。
  - 現在選択中のユーザーごとに状態セクションの追加・変更・並び替えが分かれます。
- ボード・一覧・タイムラインのタスクをダブルクリックすると、詳細を経由せず直接編集画面を開けるようにしました。
- タイムラインのタスク表示を1行にまとめました。
  - 表示順は `担当者の頭文字 / 優先度 / タイトル`
  - 担当者はフルネームではなく頭文字のみ表示します。
- `style.css?v=16` / `app.js?v=16` / 各アイコン画像 `?v=16` にしてブラウザキャッシュを回避しています。


## v17 修正内容

- タイムライン内の担当者アイコンで、頭文字が左寄りになる問題を修正しました。
  - タイムライン専用の担当者丸アイコンに対して、中央寄せを強制しています。
- タイムラインの1か月表示を修正しました。
  - 必ずその月の1日から開始します。
  - 末日は月ごとの日数に合わせます。
  - 28日・29日・30日・31日の月に対応します。
  - 前へ / 次へ は月単位で移動します。
- `style.css?v=17` / `app.js?v=17` / 各アイコン画像 `?v=17` にしてブラウザキャッシュを回避しています。


## v18 修正内容

指定された優先度番号 2,3,4,6,7,9 を実装しました。

### 2. コメント・対応履歴
- 既存のコメント機能に加え、対応履歴を詳細画面へ追加しました。
- 作成、編集、状態変更、コメント追加、チェックリスト更新、定期タスク作成などが履歴に残ります。

### 3. サブタスク / チェックリスト
- 既存チェックリストを強化し、完了・未完了変更が履歴に残るようにしました。
- ボードカード上の進捗バーも継続して表示されます。

### 4. 期限アラート / 放置タスク強調
- 期限超過、今日まで、7日以上更新なしをカード・詳細画面で強調表示します。
- 放置気味タスクは「放置気味」バッジで表示します。

### 6. ダッシュボード / 集計強化
- 左メニューに「ダッシュボード」を追加しました。
- 未完了、期限超過、今日まで、放置気味、今月完了を集計します。
- 担当者別、分類別、状態別、見落とし注意リストを表示します。

### 7. テンプレート機能
- タスク作成画面にテンプレート選択を追加しました。
- プリンタ不具合、PCキッティング、アカウント作成、電子カルテ端末確認、Web/HP修正、業者問い合わせを用意しています。

### 9. 定期タスク
- タスク作成・編集画面に繰り返し設定を追加しました。
- 毎週、毎月、毎年に対応しています。
- 定期タスクを完了すると、次回期限のタスクを自動作成します。

- `style.css?v=18` / `app.js?v=18` / 各アイコン画像 `?v=18` にしてブラウザキャッシュを回避しています。


## v19 修正内容

- 左メニューのアイコン色を再調整しました。
  - ボード：黄色
  - 一覧：青
  - タイムライン：青緑
  - ダッシュボード：紫
  - 自分の担当：藍紫
  - 完了：緑
- 左メニューの「完了」と、タスク詳細の「完了にする」ボタンの色味を緑系で統一しました。
- 左メニューの「現在のユーザー」を絞り込み欄の下へ移動しました。
- タスク作成・編集画面左上の `TASK` 表記を削除しました。
- タスク作成・編集画面のテンプレート欄に「テンプレート編集」ボタンを追加しました。
- テンプレート管理画面を追加しました。
  - テンプレートの追加
  - テンプレートの修正
  - テンプレートの削除
  - 件名、優先度、分類、タグ、内容、チェックリストを編集可能
- テンプレートはFirebaseの `rooms/{roomId}/meta/taskTemplates` に保存されます。
- テンプレートを選択しても、担当者は現在のユーザーを優先します。
- `style.css?v=19` / `app.js?v=19` / 各アイコン画像 `?v=19` にしてブラウザキャッシュを回避しています。

## v20 修正内容

- ダッシュボードが左メニューの「自分の担当」フィルターを反映するようにしました。
- これまでダッシュボードは `state.tasks` を直接集計していたため、常に全体集計になっていました。
- v20では、ボード・一覧・タイムラインと同じく `getFilteredTasks()` 後のタスクを使って集計します。
- 担当者・状態・優先度・分類・期限超過・今日まで・固定・検索キーワードもダッシュボードへ反映されます。
- ダッシュボード見出しにも、現在の対象が分かるようにしました。
- `style.css?v=20` / `app.js?v=20` / 各アイコン画像 `?v=20` にしてブラウザキャッシュを回避しています。

## v21 修正内容

- ダッシュボードで「自分の担当」をONにした際、今月完了が0件になる問題を修正しました。
- 原因は、v20でダッシュボードにも `getFilteredTasks()` を使うようにした結果、「自分の担当」時に完了済みタスクが除外されていたためです。
- v21ではダッシュボード専用の `getDashboardFilteredTasks()` を追加しました。
- これにより「自分の担当」ONでも、未完了集計は未完了のみ、今月完了は完了済みタスクも含めて正しく集計します。
- 担当者・状態・優先度・分類・検索などの絞り込みは引き続き反映されます。
- `style.css?v=21` / `app.js?v=21` / 各アイコン画像 `?v=21` にしてブラウザキャッシュを回避しています。


## v22 修正内容

- 左メニューを以下の構成へ変更しました。
  - タスク
    - ボード
    - 一覧
    - タイムライン
    - ダッシュボード
  - 予定
    - スケジュール
- スケジュール管理を小さめの機能として追加しました。
- Firebaseの保存先はタスクとは分けて `rooms/{roomId}/schedules` にしています。
- スケジュール画面では以下の範囲を切り替えできます。
  - 今日
  - 今週
  - 今月
- 予定の登録項目は以下です。
  - 件名
  - 開始日時
  - 終了日時
  - 担当者
  - 場所
  - 分類
  - メモ
  - 関連タスク
- スケジュール画面では、予定のカードをクリックすると編集できます。
- 左メニューの「自分の担当」や担当者・分類フィルターはスケジュールにも反映します。
- `style.css?v=22` / `app.js?v=22` / 各アイコン画像 `?v=22` にしてブラウザキャッシュを回避しています。


## v23 修正内容

- スケジュール登録画面で開始日時を変更した際、終了日時が自動で開始日時の1時間後になるようにしました。
- ダイアログ表示中のエラーやメッセージが背面に隠れないよう、トースト通知を開いているダイアログ内へ移動して表示するようにしました。
- スケジュール画面に表示モードを追加しました。
  - 一覧
  - 日別
  - 月カレンダー
- 予定の重複警告を追加しました。
  - 同じ担当者の予定時間が重なっている場合、保存前に確認メッセージを表示します。
- 予定からタスク作成を追加しました。
  - 保存済みの予定を開き、「この予定からタスク作成」を押すと準備タスクを作成できます。
  - タスク保存後、その予定の関連タスクとして自動で紐づけます。
- タスクから予定作成を追加しました。
  - タスク詳細の「予定を作成」から、関連タスク付きの予定を作成できます。
- `style.css?v=23` / `app.js?v=23` / 各アイコン画像 `?v=23` にしてブラウザキャッシュを回避しています。


## v24 修正内容

- 日別表示を改善しました。
  - 以前は「今週」「今月」で日別を選ぶと、週初・月初の1日だけ表示していたため分かりづらい状態でした。
  - v24では、日別表示で「今日」は1日分、「今週」は週内の日ごと、「今月」は月内の日ごとに表示します。
- スケジュールに前後移動を追加しました。
  - 日：前日 / 翌日
  - 週：前週 / 翌週
  - 月：前月 / 翌月
  - 今日へボタンで現在日に戻れます。
- 月カレンダーを前月・次月へ移動できるようにしました。
- 月カレンダーの日付セルをダブルクリックすると、その日付で予定作成画面を開けるようにしました。
  - 初期時刻は 09:00〜10:00 です。
- 予定からタスク作成時に `\n` が文字として入る問題を修正しました。
  - 内容・メモ、チェックリストともに正しい改行で入ります。
- 関連タスクへ移動できるようにしました。
  - スケジュールカードの関連タスクボタン、予定編集画面の「関連タスクを開く」から移動できます。
  - 関連タスクの担当者・完了状態に応じて、自分の担当 / 完了 / 全体の絞り込みを自動調整します。
  - 一覧画面へ移動し、検索欄に関連タスク名を入れて対象を表示します。
- `style.css?v=24` / `app.js?v=24` / 各アイコン画像 `?v=24` にしてブラウザキャッシュを回避しています。


## v25 修正内容

- スケジュール登録・編集画面で開始日時を変更した際の終了日時自動補正を調整しました。
- 終了日時が開始日時より前、または同時刻になった場合のみ、終了日時を開始日時の1時間後へ自動補正します。
- 終了日時が開始日時より未来の場合は、長時間予定として扱い、終了日時を変更しません。
- `style.css?v=25` / `app.js?v=25` / 各アイコン画像 `?v=25` にしてブラウザキャッシュを回避しています。


## v26 修正内容

- タイトル・ブラウザタブ・画面内の「タスク共有」を「タスク管理」へ変更しました。
- スケジュールの日別表示を削除しました。
  - 表示形式は「一覧」「月カレンダー」に整理しました。
- スケジュールの操作UIを分かりやすく整理しました。
  - 表示期間：今日 / 今週 / 今月
  - 表示日を移動：前へ / 今日へ / 次へ
  - 表示形式：一覧 / 月カレンダー
- タイムラインの日付・曜日に土日祝の色分けを追加しました。
  - 日曜・祝日：赤系
  - 土曜：青系
- スケジュールの一覧・月カレンダーにも土日祝の色分けを追加しました。
- タスクのボード・一覧・詳細に表示される期限へ曜日を追加しました。
  - 例：2026-07-01（水） 15:00
- `style.css?v=26` / `app.js?v=26` / 各アイコン画像 `?v=26` にしてブラウザキャッシュを回避しています。


## v27 修正内容

- 左メニューを機能単位に整理しました。
  - メニュー
    - タスク管理
    - ダッシュボード
    - スケジュール
- タスクの表示形式「ボード / 一覧 / タイムライン」は、スケジュール画面と同じようにメイン画面上部の切替UIへ移動しました。
- タイムラインで空いているセルをダブルクリックすると、日付と状態を反映したタスク作成画面が開くようにしました。
  - 日付：ダブルクリックした列の日付
  - 状態：ダブルクリックした行の状態
- 関連タスク移動時は、タスク管理の一覧表示へ移動するように調整しました。
- `style.css?v=27` / `app.js?v=27` / 各アイコン画像 `?v=27` にしてブラウザキャッシュを回避しています。


## v28 修正内容

- v27で何を押しても反応しなくなる問題を修正しました。
- 原因は、v27で追加した「ボード / 一覧 / タイムライン」の切替ボタンに対するイベント処理がある一方で、`elements.taskViewButtons` の定義が抜けていたことです。
- そのため `setupEvents()` の途中で JavaScript エラーになり、新しいタスク・メニュー・スケジュールなど後続のクリック処理が登録されていませんでした。
- v28では `taskViewButtons` を正しく定義し、念のため今後HTML構造が変わってもクラッシュしにくいよう防御処理も追加しました。
- タスク管理のボード表示についても、明示的に `renderBoard()` を呼ぶ形にして安定性を上げています。
- `style.css?v=28` / `app.js?v=28` / 各アイコン画像 `?v=28` にしてブラウザキャッシュを回避しています。


## v29 修正内容

提案した1〜15の改善をまとめて実装しました。

### 追加した主な機能

- 「今日」ビューを追加
  - 今日の予定
  - 期限超過
  - 今日までのタスク
  - 未整理ボックス
  - 空き時間にやるタスク
- クイック追加を追加
  - 件名だけで「未整理」タスクとして登録できます。
- 対応履歴を「対応履歴・コメント」に統合
  - 既存の対応履歴とコメントが分断されないよう、同じ流れで確認できます。
- コメント種別を追加
  - 作業メモ
  - 業者回答
  - 確認依頼
  - 申し送り
- 表示密度の切り替えを追加
  - 標準
  - コンパクト
- タスクカード左端に優先度色を追加
- 保存済みフィルターを追加
  - よく使う条件を保存できます。
- 一覧画面に一括操作を追加
  - 状態変更
  - 担当者変更
  - 分類変更
  - 完了
  - 削除
- タスク詳細のボタン配置を整理
  - 主要操作と補助操作を分けました。
- タスク複製を追加
- 完了時の完了メモを追加
- ナレッジ化を追加
  - 完了メモや対応内容をもとにナレッジとして保存できます。
- タスク詳細に関連予定を表示
- ヘッダー縮小ボタンを追加
- 空状態に追加ボタンを追加
  - 空欄からそのままタスク・予定追加できます。

### Firebase保存先

- タスク：`rooms/{roomId}/tasks`
- 予定：`rooms/{roomId}/schedules`
- ナレッジ：`rooms/{roomId}/knowledge`
- 保存済みフィルター：`rooms/{roomId}/meta/savedFilters`

### キャッシュ回避

- `style.css?v=29`
- `app.js?v=29`
- 各アイコン画像 `?v=29`


## v30 修正内容

- v29の全改善を含んだまま、今日ビュー内の予定カードもクリックできるようにしました。
- 今日ビューに表示された予定カードから、予定編集・関連タスク移動ができます。
- 既定の「未整理」保存フィルターを、分類ではなく検索ワードで拾う形へ変更しました。
  - 分類一覧に「未整理」が未登録でも、クイック追加した未整理タスクを確実に表示できます。
- `style.css?v=30` / `app.js?v=30` / 各アイコン画像 `?v=30` にしてブラウザキャッシュを回避しています。


## v31 修正内容

- v30で「今日 / タスク管理 / ダッシュボード / スケジュール」を押しても各画面が表示されない問題を修正しました。
- 原因は、v30で追加した保存済みフィルター用の `DEFAULT_SAVED_FILTERS` が初期化される前に `init()` が実行され、`renderSavedFilters()` で JavaScript エラーになっていたことです。
- v31では `init()` をファイル末尾へ移動し、すべての関数・定数定義が完了してから初期化するようにしました。
- 今後同様の描画エラーが起きても画面が完全な空白にならないよう、描画エラー表示を追加しました。
- クイック追加の表示を短縮しました。
  - プレースホルダー：`クイック追加`
  - ボタン：`追加`
- タスク管理のツールバーが横幅を超えて横スクロールになる問題を修正しました。
- ヘッダー縮小ボタンと共有ルーム表示が重ならないよう位置と見た目を調整しました。
- `style.css?v=31` / `app.js?v=31` / 各アイコン画像 `?v=31` にしてブラウザキャッシュを回避しています。


## v32 修正内容

- ヘッダー右上の「ヘッダー縮小」と「共有ルーム」が重ならないように再調整しました。
  - ヘッダー縮小は白いボタンへ変更
  - 共有ルームはその下へ分離
- クイック追加・検索欄・並び替えのバランスを再調整しました。
  - 入力欄が小さくなりすぎないよう、ツールバーを2段構成に整理
  - タスク管理画面では、1段目に「新規タスク / クイック追加 / 検索 / 並び替え」、2段目に「表示密度 / 表示形式」を配置
- 左メニューを画面左に固定表示しました。
  - 中央のタスク内容を下にスクロールしても、左メニューが見切れにくくなります。
  - 左メニュー自体は独立してスクロールできます。
- `style.css?v=32` / `app.js?v=32` / 各アイコン画像 `?v=32` にしてブラウザキャッシュを回避しています。


## v33 修正内容

- 「ヘッダー縮小」ボタンを削除しました。
  - 実際には `REALTIME TASK BOARD` が消えるだけで有用性が低かったため、機能ごと削除しています。
- 「表示密度」を削除しました。
  - 標準/コンパクトの差が小さく、操作項目としての価値が低かったため削除しました。
- ヘッダー右側は「共有ルーム」だけが表示されるように整理しました。
- 左メニューをページ全体のスクロールから切り離し、画面左に固定しました。
  - 中央の内容を下へスクロールしても、左メニューは常に画面左に残ります。
  - 左メニュー自体の項目が多い場合は、左メニュー内だけスクロールできます。
  - 画面下まで左メニュー背景が続くようにしました。
- 表示密度を削除した分、クイック追加と検索欄の横幅を広めに再調整しました。
- `style.css?v=33` / `app.js?v=33` / 各アイコン画像 `?v=33` にしてブラウザキャッシュを回避しています。


## v34 修正内容

- タスクカードの色の意味が分かりづらかったため、タスク管理画面に「色の意味」凡例を追加しました。
- 色のルールを整理しました。
  - 左端の線：優先度のみ
    - 緊急：赤
    - 高：オレンジ
    - 中：青
    - 低：グレー
  - 今日まで：黄色系バッジと薄い枠
  - 期限超過：赤系背景
  - 放置気味：薄いオレンジ背景
  - 固定：黄色系の枠
- これまで「今日まで」のタスクにも左端にオレンジ線が入り、優先度の色と重なって分かりづらかったため、左端の線は優先度専用に統一しました。
- タスクカードにマウスを乗せると、色の意味がツールチップで確認できます。
- `style.css?v=34` / `app.js?v=34` / 各アイコン画像 `?v=34` にしてブラウザキャッシュを回避しています。


## v35 修正内容

- タスク詳細の「対応履歴・コメント」入力欄を改善しました。
- これまで「種別 / テキスト入力 / 追加ボタン」が横並びになっていたため、右側の詳細パネル内でテキスト欄が極端に小さくなっていました。
- v35では縦並びに変更しました。
  - 1段目：種別選択
  - 2段目：コメント入力
  - 3段目：追加ボタン
- コメント入力欄の高さを広げ、複数行の作業メモ・業者回答・確認依頼を書きやすくしました。
- 大きい画面では詳細パネルを少し広めにして、読み書きしやすくしています。
- `style.css?v=35` / `app.js?v=35` / 各アイコン画像 `?v=35` にしてブラウザキャッシュを回避しています。


## v36 修正内容

- タスク詳細パネルを画面右に固定しました。
  - 中央のタスク一覧や今日ビューを下へスクロールしても、タスク詳細は右側に固定されたまま表示されます。
  - タスク詳細の中身が長い場合は、詳細パネル内だけスクロールできます。
- これまで通り、タスクをクリックした時だけ詳細が表示されます。
- これまで通り、×ボタンや何もないところをクリックした時は詳細が非表示になります。
- 詳細表示中は、中央の表示領域が右パネルの下に潜り込まないよう、右側に余白を確保するよう調整しました。
- 詳細パネル内の「タスク詳細」ヘッダーは固定表示にし、スクロールしても閉じるボタンが見えるようにしました。
- `style.css?v=36` / `app.js?v=36` / 各アイコン画像 `?v=36` にしてブラウザキャッシュを回避しています。


## v37 修正内容

- タスク詳細の「対応履歴・コメント」を改善しました。
- コメント入力欄を「対応履歴・コメント」見出しの直下へ移動しました。
  - 最新コメントが上に表示されるため、入力欄も上に置く構成にしました。
- 「コメント」と「対応履歴」をタブで分けました。
  - デフォルトはコメント表示です。
  - コメント件数・履歴件数もタブ内に表示します。
- コメント・履歴カードをコンパクト化しました。
  - 「履歴」「作業メモ」「業者回答」などの種別ラベルを独立した1行ではなく、ユーザー名の横に表示するように変更しました。
  - 1件あたりの縦幅を抑え、情報量が増えても見やすくしています。
- `style.css?v=37` / `app.js?v=37` / 各アイコン画像 `?v=37` にしてブラウザキャッシュを回避しています。


## v38 修正内容

- 表示形式・統計・色の意味を1行のコントロール行に統合しました。
  - 左：表示形式（ボード / 一覧 / タイムライン）
  - 中：統計（ダッシュボード）
  - 右：色の意味
- 左メニューから「ダッシュボード」を削除しました。
  - ダッシュボードはタスク管理画面内の「統計」から開く形に整理しています。
  - ダッシュボード表示中は、左メニューの「タスク管理」が選択状態になります。
- ナレッジ化済みのタスクでは、タスク詳細のボタンを「ナレッジ解除」に切り替えるようにしました。
- 「ナレッジ解除」を押すと、タスクとのナレッジ紐づけを解除し、作成済みのナレッジも一覧から削除します。
- `style.css?v=38` / `app.js?v=38` / 各アイコン画像 `?v=38` にしてブラウザキャッシュを回避しています。


## v39 修正内容

- サイト名称を、タスクだけでなくスケジュール管理も含む内容に合わせて変更しました。
  - 旧：タスク管理
  - 新：業務管理ボード
- ブラウザタブのタイトルを「業務管理ボード」に変更しました。
- 左上のサイト名を「業務管理ボード」に変更しました。
- ヘッダーの大タイトルを「業務管理ボード」に変更しました。
- ヘッダー上部の英語表記を「WORK MANAGEMENT BOARD」に変更しました。
- 左メニュー上部の英語表記を「WORK MANAGEMENT」に変更しました。
- 左メニュー内の機能名は、サイト全体名に合わせて「業務管理」に整理しました。
- 機能名としての「タスク」「スケジュール」は、各機能内ではそのまま残しています。
- `style.css?v=39` / `app.js?v=39` / 各アイコン画像 `?v=39` にしてブラウザキャッシュを回避しています。


## v40 修正内容

- 「表示形式・統計・色の意味」の並び順を修正しました。
  - 左：表示形式
  - 中央：統計
  - 右：色の意味
- 既存CSSの `grid-area` 指定が残っていた影響で、表示形式が右側へ飛び、2行表示になっていました。
- v40ではこの行を flex レイアウトに変更し、順番を明示固定しました。
- タスク詳細を開いた状態でも、縦に崩れにくいように、色の意味をさらにコンパクト化しています。
- 色説明の文言を短縮しました。
  - 今日まで → 今日
  - 放置気味 → 放置
- `style.css?v=40` / `app.js?v=40` / 各アイコン画像 `?v=40` にしてブラウザキャッシュを回避しています。


## v41 修正内容

- 検索が機能しない画面では、検索欄を表示しないようにしました。
- 今日ビューでは、検索欄と並び替えを非表示にしました。
  - 今日ビューは「今日やること」を固定の考え方でまとめる画面のため、検索・並び替えは出さず、`新しいタスク` と `クイック追加` を広めに表示します。
- ダッシュボードでは、上部ツールバー自体を非表示にしました。
  - ダッシュボードは集計・分析画面のため、検索・クイック追加・並び替えは出さない構成にしました。
  - ダッシュボードの集計では、非表示の検索文字列に影響されないように修正しました。
- スケジュールでは、予定検索として機能する検索欄だけ残しました。
  - `新しいタスク`
  - `クイック追加`
  - `おすすめ順`
  はスケジュール画面では非表示にしました。
- スケジュール画面の検索欄プレースホルダーを「予定名・メモ・場所・分類で検索」に変更しました。
- `style.css?v=41` / `app.js?v=41` / 各アイコン画像 `?v=41` にしてブラウザキャッシュを回避しています。
