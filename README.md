# システム課タスク共有

システム担当2名向けのリアルタイムタスク共有サイトです。GitHub Pages + Firebase Realtime Database で動作します。

## 主な機能

- Firebase Realtime Databaseによる共同編集
- ルームID付きURLで複数ルーム運用
- ユーザー選択、ユーザー追加、ユーザーカラー
- タスク追加・編集・削除
- 状態：未着手 / 対応中 / 確認待ち / 保留 / 完了
- 優先度：緊急 / 高 / 中 / 低
- 分類：PC / プリンタ / ネットワーク / 電子カルテ / Web/HP / アカウント / 業者対応 / 定期作業 / その他
- 期限日・期限時刻
- 依頼元
- タグ
- チェックリスト
- コメント・申し送り
- 固定表示
- 期限超過、今日まで、自分の担当、固定のみフィルター
- ボード表示 / 一覧表示 / 自分の担当 / 完了表示
- Firebase未設定時のローカル保存フォールバック

## GitHub Pagesへのアップロード

1. このzipを解凍します。
2. 中身をGitHubリポジトリへアップロードします。
3. `config.js` にFirebase設定を貼り付けます。
4. GitHub Pagesを有効化します。
5. 公開URLへアクセスします。

## Firebase Realtime Database設定

### 1. Firebaseプロジェクト作成

Firebase Consoleで新規プロジェクトを作成します。Google Analyticsは不要ならOFFで構いません。

### 2. Webアプリ追加

プロジェクト概要からWebアプリを追加し、表示された `firebaseConfig` を `config.js` に貼り付けます。

### 3. Realtime Database作成

Realtime Databaseを作成します。場所は近い地域で構いません。最初はテストモードでも動きますが、公開前にルールを確認してください。

### 4. ルール設定

Realtime Database の「ルール」タブに `firebase-rules.json` の内容を貼り付けて公開します。

注意：このルールは、URLを知っている人が読み書きできる簡易運用向けです。院内の実名・患者情報・パスワード・IPアドレスなどの機密情報は入力しないでください。認証付きにしたい場合はFirebase Authenticationを追加してください。

## config.js の例

```js
window.firebaseConfig = {
  apiKey: "xxxx",
  authDomain: "xxxx.firebaseapp.com",
  databaseURL: "https://xxxx-default-rtdb.firebaseio.com",
  projectId: "xxxx",
  storageBucket: "xxxx.firebasestorage.app",
  messagingSenderId: "xxxx",
  appId: "xxxx"
};
```

## データ構造

```text
rooms/{roomId}/meta
rooms/{roomId}/tasks/{taskId}
```

## 共有方法

画面右上の「共有リンク」を押すと、現在のルームID付きURLをコピーできます。
同じURLを2人で開くと同じタスクを共有できます。


## v2 修正内容

- `config.js` にFirebase設定を反映しました。
- Firebase Consoleから取得したコードに含まれる `import` / `initializeApp` / `getAnalytics` は削除しました。
- このサイトでは `app.js` 側でFirebase SDKを読み込むため、`config.js` には `window.firebaseConfig = {...}` のみ記載します。
- Realtime Database用に `databaseURL` を追加しました。
- `databaseURL` は `https://task-kanri-2ad16-default-rtdb.firebaseio.com` としています。
  - Firebase ConsoleのRealtime Database画面に表示されるURLが異なる場合は、`config.js` の `databaseURL` だけ差し替えてください。


## v3 修正内容

- 初期ユーザー（福冨・森井）も削除できるようにしました。
- タイトルを「システム課タスク共有」から「タスク共有」に変更しました。
- ヒーロー右側の「共有リンク」ボタンを削除しました。
- 「＋ 新しいタスク」ボタンを検索バー左側へ移動しました。
- 左上の「管」アイコンをユーザー提供画像へ変更しました。
- 統計カードと左メニューのアイコンをユーザー提供画像へ変更しました。
- 分類管理画面を追加しました。
  - 分類の追加
  - 分類名の変更
  - 分類の削除
  - 既存タスクに使われている分類を変更/削除した場合、タスク側も反映
- 分類一覧はFirebaseの `rooms/{roomId}/meta/categories` に保存されます。


## v4 修正内容

- タイトル枠の高さを少し小さくしました。
- 共有ルーム表示をタイトル左下ではなく、タイトル枠の右下へ移動しました。
- 右側の空きスペースを活かす配置に変更しています。
- 画面幅が狭い場合は、共有ルーム表示がタイトル下へ自然に回り込むようにしています。
- `style.css?v=4` / `app.js?v=4` にしてブラウザキャッシュを回避しています。


## v5 修正内容

- タスク詳細の「完了にする」ボタンを緑の強調ボタンへ変更しました。
- 左メニューのアイコン画像を少し大きくしました。
- 左上のブランドアイコンを、ユーザー提供の3枚目アイコンへ変更しました。
- favicon / apple-touch-icon も同じ3枚目アイコンへ差し替えました。
- `style.css?v=5` / `app.js?v=5` / 各アイコン画像 `?v=5` にしてブラウザキャッシュを回避しています。


## v6 修正内容

- 一覧表示時に、ボード用の「未着手・対応中・確認待ち・保留」列が出ないように整理しました。
- 左メニューを2グループ構成に変更しました。
  - 表示：ボード / 一覧
  - 絞り込み：自分の担当 / 完了
- 「ボード表示で自分の担当」「一覧表示で自分の担当」「ボード表示で完了」「一覧表示で完了」のように、表示形式と絞り込みを組み合わせられるようにしました。
- タスク詳細に「作成日」と「更新者」を追加しました。
- 左上のタスク共有アイコンとブラウザタブアイコンを、サイトの雰囲気に合わせた新しい角丸アイコンへ変更しました。
- `style.css?v=6` / `app.js?v=6` / 各アイコン画像 `?v=6` にしてブラウザキャッシュを回避しています。


## v7 修正内容

- 状態セクションを追加・名称変更・削除できるようにしました。
  - ボード画面の右端に「＋ セクション追加」を追加
  - 左メニューの状態フィルター横に「編集」を追加
  - 状態はFirebaseの `rooms/{roomId}/meta/statuses` に保存
- 追加した状態は以下へ反映されます。
  - ボードの状態カラム
  - タスク追加/編集画面の状態プルダウン
  - 左メニューの状態フィルター
- `完了` は完了ボタンで使用するため、削除不可の固定状態にしています。
- タイムライン表示を追加しました。
  - 左メニューの「表示」グループに `タイムライン` を追加
  - 14日分の期限日ベースで表示
  - 前へ / 今日 / 次へ の移動ボタン
  - タイムライン上のタスククリックで詳細表示
  - 期限なしタスクの別枠表示
- タイムライン用の新しいアイコンを追加しました。
- `style.css?v=7` / `app.js?v=7` / 各アイコン画像 `?v=7` にしてブラウザキャッシュを回避しています。


## v8 修正内容

- 左メニューのボード・一覧・自分の担当・完了アイコンを再作成し、白いフチが見えないようにしました。
- タイムラインのアイコンを、よりシンプルで分かりやすい横軸アイコンに変更しました。
- タイムライン内のユーザー丸アイコンの文字が中央に来るようCSSを修正しました。
- 状態管理で、状態の順番を上下ボタンで入れ替えられるようにしました。
- 分類管理で、分類の順番を上下ボタンで入れ替えられるようにしました。
- 状態を新規追加した場合は、通常運用で扱いやすいように「完了」の手前へ追加されます。
- `style.css?v=8` / `app.js?v=8` / 各アイコン画像 `?v=8` にしてブラウザキャッシュを回避しています。


## v9 修正内容

- 状態セクションが6個以上になっても下段へ折り返さず、横スクロールで右側に並ぶようにしました。
- ボード表示の状態カラムをCSS Gridから横並びFlex表示に上書きしています。
- 横スクロールバーの見た目も少し調整しました。
- スマホ幅でも縦に折り返さず、横スクロールで確認できるようにしています。
- `style.css?v=9` / `app.js?v=9` / 各アイコン画像 `?v=9` にしてブラウザキャッシュを回避しています。


## v12 修正内容

- v10/v11で発生していた表示停止・ボタン無反応の原因を再調査し、v9をベースに安全に作り直しました。
- 原因はv10のドラッグ対応追加時に、関数の置換範囲が崩れ、JavaScriptの構造が壊れていたことです。
- 今回は動作していたv9から再実装しています。
- 状態管理画面で、状態をドラッグ＆ドロップで並び替えできるようにしました。
- 分類管理画面で、分類をドラッグ＆ドロップで並び替えできるようにしました。
- ユーザー管理画面で、ユーザーをドラッグ＆ドロップで並び替えできるようにしました。
- ボード画面の状態セクションも、見出し左のハンドルをドラッグして並び替えできるようにしました。
- 以前の上下ボタンで1つずつ移動するUIは非表示にしています。
- `style.css?v=12` / `app.js?v=12` / 各アイコン画像 `?v=12` にしてブラウザキャッシュを回避しています。


## v13 修正内容

- 「おすすめ順」の並びを実務向けに修正しました。
- 以前は `固定 → 優先度 → 期限` が中心でした。
- v13では以下の順番で表示します。
  1. 固定表示
  2. 期限超過
  3. 今日まで
  4. 優先度
  5. 期限が近い
  6. 更新が新しい
  7. 期限なし
- 優先度だけでなく、期限超過・今日期限を強く上に出すようにしています。
- 同条件の場合はタスク名順で安定して並ぶようにしました。
- `style.css?v=13` / `app.js?v=13` / 各アイコン画像 `?v=13` にしてブラウザキャッシュを回避しています。


## v14 修正内容

- タスク詳細パネルを、通常時は非表示にしました。
- タスクをクリックした時だけ、これまで通り右側に詳細パネルが表示されます。
- 詳細パネルを閉じる方法を追加しました。
  - 右上の `×` をクリック
  - 画面中央の何もない場所をクリック
- 詳細パネルが閉じている間は、中央のタイトル・統計・ボード・一覧・タイムライン領域が右側まで広がります。
- 詳細パネルが開いている時だけ、右側に360pxの詳細領域を確保します。
- 狭い画面では、詳細パネルが右側から重なる形で表示されます。
- `style.css?v=14` / `app.js?v=14` / 各アイコン画像 `?v=14` にしてブラウザキャッシュを回避しています。


## v15 修正内容

- タスク詳細を非表示にしてメイン領域が広がった時、新しいタスクボタンが横長になりすぎる問題を修正しました。
- `＋ 新しいタスク` ボタンは固定幅寄りにし、検索欄が広がるように調整しました。
- 通常幅では `新しいタスク 260px / 検索欄 可変 / 並び順 220px` の比率にしています。
- 画面幅が狭い場合は、これまで通り1列表示へ切り替わります。
- `style.css?v=15` / `app.js?v=15` / 各アイコン画像 `?v=15` にしてブラウザキャッシュを回避しています。


## v16 修正内容

- タイムラインに `14日 / 1か月` の切り替えを追加しました。
  - 1か月は31日分を横スクロールで表示します。
  - 前へ / 次へ は、14日表示では7日単位、1か月表示では31日単位で移動します。
- 状態セクションをユーザーごとに管理できるようにしました。
  - 状態一覧は `rooms/{roomId}/meta/statusesByUser` に保存されます。
  - 旧形式の `meta/statuses` がある場合はフォールバックとして読み込みます。
  - 現在選択中のユーザーごとに状態セクションの追加・変更・並び替えが分かれます。
- ボード・一覧・タイムラインのタスクをダブルクリックすると、詳細を経由せず直接編集画面を開けるようにしました。
- タイムラインのタスク表示を1行にまとめました。
  - 表示順は `担当者の頭文字 / 優先度 / タイトル`
  - 担当者はフルネームではなく頭文字のみ表示します。
- `style.css?v=16` / `app.js?v=16` / 各アイコン画像 `?v=16` にしてブラウザキャッシュを回避しています。


## v17 修正内容

- タイムライン内の担当者アイコンで、頭文字が左寄りになる問題を修正しました。
  - タイムライン専用の担当者丸アイコンに対して、中央寄せを強制しています。
- タイムラインの1か月表示を修正しました。
  - 必ずその月の1日から開始します。
  - 末日は月ごとの日数に合わせます。
  - 28日・29日・30日・31日の月に対応します。
  - 前へ / 次へ は月単位で移動します。
- `style.css?v=17` / `app.js?v=17` / 各アイコン画像 `?v=17` にしてブラウザキャッシュを回避しています。


## v18 修正内容

指定された優先度番号 2,3,4,6,7,9 を実装しました。

### 2. コメント・対応履歴
- 既存のコメント機能に加え、対応履歴を詳細画面へ追加しました。
- 作成、編集、状態変更、コメント追加、チェックリスト更新、定期タスク作成などが履歴に残ります。

### 3. サブタスク / チェックリスト
- 既存チェックリストを強化し、完了・未完了変更が履歴に残るようにしました。
- ボードカード上の進捗バーも継続して表示されます。

### 4. 期限アラート / 放置タスク強調
- 期限超過、今日まで、7日以上更新なしをカード・詳細画面で強調表示します。
- 放置気味タスクは「放置気味」バッジで表示します。

### 6. ダッシュボード / 集計強化
- 左メニューに「ダッシュボード」を追加しました。
- 未完了、期限超過、今日まで、放置気味、今月完了を集計します。
- 担当者別、分類別、状態別、見落とし注意リストを表示します。

### 7. テンプレート機能
- タスク作成画面にテンプレート選択を追加しました。
- プリンタ不具合、PCキッティング、アカウント作成、電子カルテ端末確認、Web/HP修正、業者問い合わせを用意しています。

### 9. 定期タスク
- タスク作成・編集画面に繰り返し設定を追加しました。
- 毎週、毎月、毎年に対応しています。
- 定期タスクを完了すると、次回期限のタスクを自動作成します。

- `style.css?v=18` / `app.js?v=18` / 各アイコン画像 `?v=18` にしてブラウザキャッシュを回避しています。


## v19 修正内容

- 左メニューのアイコン色を再調整しました。
  - ボード：黄色
  - 一覧：青
  - タイムライン：青緑
  - ダッシュボード：紫
  - 自分の担当：藍紫
  - 完了：緑
- 左メニューの「完了」と、タスク詳細の「完了にする」ボタンの色味を緑系で統一しました。
- 左メニューの「現在のユーザー」を絞り込み欄の下へ移動しました。
- タスク作成・編集画面左上の `TASK` 表記を削除しました。
- タスク作成・編集画面のテンプレート欄に「テンプレート編集」ボタンを追加しました。
- テンプレート管理画面を追加しました。
  - テンプレートの追加
  - テンプレートの修正
  - テンプレートの削除
  - 件名、優先度、分類、タグ、内容、チェックリストを編集可能
- テンプレートはFirebaseの `rooms/{roomId}/meta/taskTemplates` に保存されます。
- テンプレートを選択しても、担当者は現在のユーザーを優先します。
- `style.css?v=19` / `app.js?v=19` / 各アイコン画像 `?v=19` にしてブラウザキャッシュを回避しています。

## v20 修正内容

- ダッシュボードが左メニューの「自分の担当」フィルターを反映するようにしました。
- これまでダッシュボードは `state.tasks` を直接集計していたため、常に全体集計になっていました。
- v20では、ボード・一覧・タイムラインと同じく `getFilteredTasks()` 後のタスクを使って集計します。
- 担当者・状態・優先度・分類・期限超過・今日まで・固定・検索キーワードもダッシュボードへ反映されます。
- ダッシュボード見出しにも、現在の対象が分かるようにしました。
- `style.css?v=20` / `app.js?v=20` / 各アイコン画像 `?v=20` にしてブラウザキャッシュを回避しています。

## v21 修正内容

- ダッシュボードで「自分の担当」をONにした際、今月完了が0件になる問題を修正しました。
- 原因は、v20でダッシュボードにも `getFilteredTasks()` を使うようにした結果、「自分の担当」時に完了済みタスクが除外されていたためです。
- v21ではダッシュボード専用の `getDashboardFilteredTasks()` を追加しました。
- これにより「自分の担当」ONでも、未完了集計は未完了のみ、今月完了は完了済みタスクも含めて正しく集計します。
- 担当者・状態・優先度・分類・検索などの絞り込みは引き続き反映されます。
- `style.css?v=21` / `app.js?v=21` / 各アイコン画像 `?v=21` にしてブラウザキャッシュを回避しています。


## v22 修正内容

- 左メニューを以下の構成へ変更しました。
  - タスク
    - ボード
    - 一覧
    - タイムライン
    - ダッシュボード
  - 予定
    - スケジュール
- スケジュール管理を小さめの機能として追加しました。
- Firebaseの保存先はタスクとは分けて `rooms/{roomId}/schedules` にしています。
- スケジュール画面では以下の範囲を切り替えできます。
  - 今日
  - 今週
  - 今月
- 予定の登録項目は以下です。
  - 件名
  - 開始日時
  - 終了日時
  - 担当者
  - 場所
  - 分類
  - メモ
  - 関連タスク
- スケジュール画面では、予定のカードをクリックすると編集できます。
- 左メニューの「自分の担当」や担当者・分類フィルターはスケジュールにも反映します。
- `style.css?v=22` / `app.js?v=22` / 各アイコン画像 `?v=22` にしてブラウザキャッシュを回避しています。


## v23 修正内容

- スケジュール登録画面で開始日時を変更した際、終了日時が自動で開始日時の1時間後になるようにしました。
- ダイアログ表示中のエラーやメッセージが背面に隠れないよう、トースト通知を開いているダイアログ内へ移動して表示するようにしました。
- スケジュール画面に表示モードを追加しました。
  - 一覧
  - 日別
  - 月カレンダー
- 予定の重複警告を追加しました。
  - 同じ担当者の予定時間が重なっている場合、保存前に確認メッセージを表示します。
- 予定からタスク作成を追加しました。
  - 保存済みの予定を開き、「この予定からタスク作成」を押すと準備タスクを作成できます。
  - タスク保存後、その予定の関連タスクとして自動で紐づけます。
- タスクから予定作成を追加しました。
  - タスク詳細の「予定を作成」から、関連タスク付きの予定を作成できます。
- `style.css?v=23` / `app.js?v=23` / 各アイコン画像 `?v=23` にしてブラウザキャッシュを回避しています。


## v24 修正内容

- 日別表示を改善しました。
  - 以前は「今週」「今月」で日別を選ぶと、週初・月初の1日だけ表示していたため分かりづらい状態でした。
  - v24では、日別表示で「今日」は1日分、「今週」は週内の日ごと、「今月」は月内の日ごとに表示します。
- スケジュールに前後移動を追加しました。
  - 日：前日 / 翌日
  - 週：前週 / 翌週
  - 月：前月 / 翌月
  - 今日へボタンで現在日に戻れます。
- 月カレンダーを前月・次月へ移動できるようにしました。
- 月カレンダーの日付セルをダブルクリックすると、その日付で予定作成画面を開けるようにしました。
  - 初期時刻は 09:00〜10:00 です。
- 予定からタスク作成時に `\n` が文字として入る問題を修正しました。
  - 内容・メモ、チェックリストともに正しい改行で入ります。
- 関連タスクへ移動できるようにしました。
  - スケジュールカードの関連タスクボタン、予定編集画面の「関連タスクを開く」から移動できます。
  - 関連タスクの担当者・完了状態に応じて、自分の担当 / 完了 / 全体の絞り込みを自動調整します。
  - 一覧画面へ移動し、検索欄に関連タスク名を入れて対象を表示します。
- `style.css?v=24` / `app.js?v=24` / 各アイコン画像 `?v=24` にしてブラウザキャッシュを回避しています。


## v25 修正内容

- スケジュール登録・編集画面で開始日時を変更した際の終了日時自動補正を調整しました。
- 終了日時が開始日時より前、または同時刻になった場合のみ、終了日時を開始日時の1時間後へ自動補正します。
- 終了日時が開始日時より未来の場合は、長時間予定として扱い、終了日時を変更しません。
- `style.css?v=25` / `app.js?v=25` / 各アイコン画像 `?v=25` にしてブラウザキャッシュを回避しています。


## v26 修正内容

- タイトル・ブラウザタブ・画面内の「タスク共有」を「タスク管理」へ変更しました。
- スケジュールの日別表示を削除しました。
  - 表示形式は「一覧」「月カレンダー」に整理しました。
- スケジュールの操作UIを分かりやすく整理しました。
  - 表示期間：今日 / 今週 / 今月
  - 表示日を移動：前へ / 今日へ / 次へ
  - 表示形式：一覧 / 月カレンダー
- タイムラインの日付・曜日に土日祝の色分けを追加しました。
  - 日曜・祝日：赤系
  - 土曜：青系
- スケジュールの一覧・月カレンダーにも土日祝の色分けを追加しました。
- タスクのボード・一覧・詳細に表示される期限へ曜日を追加しました。
  - 例：2026-07-01（水） 15:00
- `style.css?v=26` / `app.js?v=26` / 各アイコン画像 `?v=26` にしてブラウザキャッシュを回避しています。


## v27 修正内容

- 左メニューを機能単位に整理しました。
  - メニュー
    - タスク管理
    - ダッシュボード
    - スケジュール
- タスクの表示形式「ボード / 一覧 / タイムライン」は、スケジュール画面と同じようにメイン画面上部の切替UIへ移動しました。
- タイムラインで空いているセルをダブルクリックすると、日付と状態を反映したタスク作成画面が開くようにしました。
  - 日付：ダブルクリックした列の日付
  - 状態：ダブルクリックした行の状態
- 関連タスク移動時は、タスク管理の一覧表示へ移動するように調整しました。
- `style.css?v=27` / `app.js?v=27` / 各アイコン画像 `?v=27` にしてブラウザキャッシュを回避しています。


## v28 修正内容

- v27で何を押しても反応しなくなる問題を修正しました。
- 原因は、v27で追加した「ボード / 一覧 / タイムライン」の切替ボタンに対するイベント処理がある一方で、`elements.taskViewButtons` の定義が抜けていたことです。
- そのため `setupEvents()` の途中で JavaScript エラーになり、新しいタスク・メニュー・スケジュールなど後続のクリック処理が登録されていませんでした。
- v28では `taskViewButtons` を正しく定義し、念のため今後HTML構造が変わってもクラッシュしにくいよう防御処理も追加しました。
- タスク管理のボード表示についても、明示的に `renderBoard()` を呼ぶ形にして安定性を上げています。
- `style.css?v=28` / `app.js?v=28` / 各アイコン画像 `?v=28` にしてブラウザキャッシュを回避しています。


## v29 修正内容

提案した1〜15の改善をまとめて実装しました。

### 追加した主な機能

- 「今日」ビューを追加
  - 今日の予定
  - 期限超過
  - 今日までのタスク
  - 未整理ボックス
  - 空き時間にやるタスク
- クイック追加を追加
  - 件名だけで「未整理」タスクとして登録できます。
- 対応履歴を「対応履歴・コメント」に統合
  - 既存の対応履歴とコメントが分断されないよう、同じ流れで確認できます。
- コメント種別を追加
  - 作業メモ
  - 業者回答
  - 確認依頼
  - 申し送り
- 表示密度の切り替えを追加
  - 標準
  - コンパクト
- タスクカード左端に優先度色を追加
- 保存済みフィルターを追加
  - よく使う条件を保存できます。
- 一覧画面に一括操作を追加
  - 状態変更
  - 担当者変更
  - 分類変更
  - 完了
  - 削除
- タスク詳細のボタン配置を整理
  - 主要操作と補助操作を分けました。
- タスク複製を追加
- 完了時の完了メモを追加
- ナレッジ化を追加
  - 完了メモや対応内容をもとにナレッジとして保存できます。
- タスク詳細に関連予定を表示
- ヘッダー縮小ボタンを追加
- 空状態に追加ボタンを追加
  - 空欄からそのままタスク・予定追加できます。

### Firebase保存先

- タスク：`rooms/{roomId}/tasks`
- 予定：`rooms/{roomId}/schedules`
- ナレッジ：`rooms/{roomId}/knowledge`
- 保存済みフィルター：`rooms/{roomId}/meta/savedFilters`

### キャッシュ回避

- `style.css?v=29`
- `app.js?v=29`
- 各アイコン画像 `?v=29`


## v30 修正内容

- v29の全改善を含んだまま、今日ビュー内の予定カードもクリックできるようにしました。
- 今日ビューに表示された予定カードから、予定編集・関連タスク移動ができます。
- 既定の「未整理」保存フィルターを、分類ではなく検索ワードで拾う形へ変更しました。
  - 分類一覧に「未整理」が未登録でも、クイック追加した未整理タスクを確実に表示できます。
- `style.css?v=30` / `app.js?v=30` / 各アイコン画像 `?v=30` にしてブラウザキャッシュを回避しています。


## v31 修正内容

- v30で「今日 / タスク管理 / ダッシュボード / スケジュール」を押しても各画面が表示されない問題を修正しました。
- 原因は、v30で追加した保存済みフィルター用の `DEFAULT_SAVED_FILTERS` が初期化される前に `init()` が実行され、`renderSavedFilters()` で JavaScript エラーになっていたことです。
- v31では `init()` をファイル末尾へ移動し、すべての関数・定数定義が完了してから初期化するようにしました。
- 今後同様の描画エラーが起きても画面が完全な空白にならないよう、描画エラー表示を追加しました。
- クイック追加の表示を短縮しました。
  - プレースホルダー：`クイック追加`
  - ボタン：`追加`
- タスク管理のツールバーが横幅を超えて横スクロールになる問題を修正しました。
- ヘッダー縮小ボタンと共有ルーム表示が重ならないよう位置と見た目を調整しました。
- `style.css?v=31` / `app.js?v=31` / 各アイコン画像 `?v=31` にしてブラウザキャッシュを回避しています。


## v32 修正内容

- ヘッダー右上の「ヘッダー縮小」と「共有ルーム」が重ならないように再調整しました。
  - ヘッダー縮小は白いボタンへ変更
  - 共有ルームはその下へ分離
- クイック追加・検索欄・並び替えのバランスを再調整しました。
  - 入力欄が小さくなりすぎないよう、ツールバーを2段構成に整理
  - タスク管理画面では、1段目に「新規タスク / クイック追加 / 検索 / 並び替え」、2段目に「表示密度 / 表示形式」を配置
- 左メニューを画面左に固定表示しました。
  - 中央のタスク内容を下にスクロールしても、左メニューが見切れにくくなります。
  - 左メニュー自体は独立してスクロールできます。
- `style.css?v=32` / `app.js?v=32` / 各アイコン画像 `?v=32` にしてブラウザキャッシュを回避しています。


## v33 修正内容

- 「ヘッダー縮小」ボタンを削除しました。
  - 実際には `REALTIME TASK BOARD` が消えるだけで有用性が低かったため、機能ごと削除しています。
- 「表示密度」を削除しました。
  - 標準/コンパクトの差が小さく、操作項目としての価値が低かったため削除しました。
- ヘッダー右側は「共有ルーム」だけが表示されるように整理しました。
- 左メニューをページ全体のスクロールから切り離し、画面左に固定しました。
  - 中央の内容を下へスクロールしても、左メニューは常に画面左に残ります。
  - 左メニュー自体の項目が多い場合は、左メニュー内だけスクロールできます。
  - 画面下まで左メニュー背景が続くようにしました。
- 表示密度を削除した分、クイック追加と検索欄の横幅を広めに再調整しました。
- `style.css?v=33` / `app.js?v=33` / 各アイコン画像 `?v=33` にしてブラウザキャッシュを回避しています。


## v34 修正内容

- タスクカードの色の意味が分かりづらかったため、タスク管理画面に「色の意味」凡例を追加しました。
- 色のルールを整理しました。
  - 左端の線：優先度のみ
    - 緊急：赤
    - 高：オレンジ
    - 中：青
    - 低：グレー
  - 今日まで：黄色系バッジと薄い枠
  - 期限超過：赤系背景
  - 放置気味：薄いオレンジ背景
  - 固定：黄色系の枠
- これまで「今日まで」のタスクにも左端にオレンジ線が入り、優先度の色と重なって分かりづらかったため、左端の線は優先度専用に統一しました。
- タスクカードにマウスを乗せると、色の意味がツールチップで確認できます。
- `style.css?v=34` / `app.js?v=34` / 各アイコン画像 `?v=34` にしてブラウザキャッシュを回避しています。


## v35 修正内容

- タスク詳細の「対応履歴・コメント」入力欄を改善しました。
- これまで「種別 / テキスト入力 / 追加ボタン」が横並びになっていたため、右側の詳細パネル内でテキスト欄が極端に小さくなっていました。
- v35では縦並びに変更しました。
  - 1段目：種別選択
  - 2段目：コメント入力
  - 3段目：追加ボタン
- コメント入力欄の高さを広げ、複数行の作業メモ・業者回答・確認依頼を書きやすくしました。
- 大きい画面では詳細パネルを少し広めにして、読み書きしやすくしています。
- `style.css?v=35` / `app.js?v=35` / 各アイコン画像 `?v=35` にしてブラウザキャッシュを回避しています。


## v36 修正内容

- タスク詳細パネルを画面右に固定しました。
  - 中央のタスク一覧や今日ビューを下へスクロールしても、タスク詳細は右側に固定されたまま表示されます。
  - タスク詳細の中身が長い場合は、詳細パネル内だけスクロールできます。
- これまで通り、タスクをクリックした時だけ詳細が表示されます。
- これまで通り、×ボタンや何もないところをクリックした時は詳細が非表示になります。
- 詳細表示中は、中央の表示領域が右パネルの下に潜り込まないよう、右側に余白を確保するよう調整しました。
- 詳細パネル内の「タスク詳細」ヘッダーは固定表示にし、スクロールしても閉じるボタンが見えるようにしました。
- `style.css?v=36` / `app.js?v=36` / 各アイコン画像 `?v=36` にしてブラウザキャッシュを回避しています。


## v37 修正内容

- タスク詳細の「対応履歴・コメント」を改善しました。
- コメント入力欄を「対応履歴・コメント」見出しの直下へ移動しました。
  - 最新コメントが上に表示されるため、入力欄も上に置く構成にしました。
- 「コメント」と「対応履歴」をタブで分けました。
  - デフォルトはコメント表示です。
  - コメント件数・履歴件数もタブ内に表示します。
- コメント・履歴カードをコンパクト化しました。
  - 「履歴」「作業メモ」「業者回答」などの種別ラベルを独立した1行ではなく、ユーザー名の横に表示するように変更しました。
  - 1件あたりの縦幅を抑え、情報量が増えても見やすくしています。
- `style.css?v=37` / `app.js?v=37` / 各アイコン画像 `?v=37` にしてブラウザキャッシュを回避しています。


## v38 修正内容

- 表示形式・統計・色の意味を1行のコントロール行に統合しました。
  - 左：表示形式（ボード / 一覧 / タイムライン）
  - 中：統計（ダッシュボード）
  - 右：色の意味
- 左メニューから「ダッシュボード」を削除しました。
  - ダッシュボードはタスク管理画面内の「統計」から開く形に整理しています。
  - ダッシュボード表示中は、左メニューの「タスク管理」が選択状態になります。
- ナレッジ化済みのタスクでは、タスク詳細のボタンを「ナレッジ解除」に切り替えるようにしました。
- 「ナレッジ解除」を押すと、タスクとのナレッジ紐づけを解除し、作成済みのナレッジも一覧から削除します。
- `style.css?v=38` / `app.js?v=38` / 各アイコン画像 `?v=38` にしてブラウザキャッシュを回避しています。


## v39 修正内容

- サイト名称を、タスクだけでなくスケジュール管理も含む内容に合わせて変更しました。
  - 旧：タスク管理
  - 新：業務管理ボード
- ブラウザタブのタイトルを「業務管理ボード」に変更しました。
- 左上のサイト名を「業務管理ボード」に変更しました。
- ヘッダーの大タイトルを「業務管理ボード」に変更しました。
- ヘッダー上部の英語表記を「WORK MANAGEMENT BOARD」に変更しました。
- 左メニュー上部の英語表記を「WORK MANAGEMENT」に変更しました。
- 左メニュー内の機能名は、サイト全体名に合わせて「業務管理」に整理しました。
- 機能名としての「タスク」「スケジュール」は、各機能内ではそのまま残しています。
- `style.css?v=39` / `app.js?v=39` / 各アイコン画像 `?v=39` にしてブラウザキャッシュを回避しています。


## v40 修正内容

- 「表示形式・統計・色の意味」の並び順を修正しました。
  - 左：表示形式
  - 中央：統計
  - 右：色の意味
- 既存CSSの `grid-area` 指定が残っていた影響で、表示形式が右側へ飛び、2行表示になっていました。
- v40ではこの行を flex レイアウトに変更し、順番を明示固定しました。
- タスク詳細を開いた状態でも、縦に崩れにくいように、色の意味をさらにコンパクト化しています。
- 色説明の文言を短縮しました。
  - 今日まで → 今日
  - 放置気味 → 放置
- `style.css?v=40` / `app.js?v=40` / 各アイコン画像 `?v=40` にしてブラウザキャッシュを回避しています。


## v41 修正内容

- 検索が機能しない画面では、検索欄を表示しないようにしました。
- 今日ビューでは、検索欄と並び替えを非表示にしました。
  - 今日ビューは「今日やること」を固定の考え方でまとめる画面のため、検索・並び替えは出さず、`新しいタスク` と `クイック追加` を広めに表示します。
- ダッシュボードでは、上部ツールバー自体を非表示にしました。
  - ダッシュボードは集計・分析画面のため、検索・クイック追加・並び替えは出さない構成にしました。
  - ダッシュボードの集計では、非表示の検索文字列に影響されないように修正しました。
- スケジュールでは、予定検索として機能する検索欄だけ残しました。
  - `新しいタスク`
  - `クイック追加`
  - `おすすめ順`
  はスケジュール画面では非表示にしました。
- スケジュール画面の検索欄プレースホルダーを「予定名・メモ・場所・分類で検索」に変更しました。
- `style.css?v=41` / `app.js?v=41` / 各アイコン画像 `?v=41` にしてブラウザキャッシュを回避しています。


## v42 修正内容

- タスク管理画面の「色の意味」を右側配置に修正しました。
  - 表示形式・統計は左側にまとめ、色説明は行の右側に寄せています。
- 検索欄のUI崩れを修正しました。
  - 虫眼鏡だけが1行目、検索文字が2行目に分かれていた状態を修正しました。
  - 検索欄は横並びの1行入力UIにしています。
- タスク管理画面では検索欄の横幅を少し広めに確保しました。
- タスク詳細表示中でも検索欄が崩れにくいように調整しました。
- `style.css?v=42` / `app.js?v=42` / 各アイコン画像 `?v=42` にしてブラウザキャッシュを回避しています。


## v43 修正内容

- 左メニューのアイコン色を機能別に整理しました。
- これまで「今日・業務管理・スケジュール」が黄色/オレンジ系に寄りすぎていたため、以下の配色に変更しています。
  - 今日：アンバー
  - 業務管理：ブルー
  - スケジュール：コーラル
  - 自分の担当：パープル
  - 完了：グリーン
- サイト全体の青緑系トーンとは馴染ませつつ、メニュー内で視覚的に区別しやすい配色にしています。
- 選択中の黄色ボタン上でもアイコンが埋もれないよう、アイコンの白枠と影を調整しました。
- `style.css?v=43` / `app.js?v=43` / 各アイコン画像 `?v=43` にしてブラウザキャッシュを回避しています。


## v44 修正内容

- 左メニューの「よく使う表示」を削除しました。
  - 実用性が低かったため、画面から取り除いて左メニューをすっきりさせています。
- v43で左メニューのアイコンがすべて白くなっていた問題を修正しました。
  - 原因は、アイコン画像に `filter: brightness(0) invert(1)` をかけていたことです。
  - v44ではフィルターを解除し、PNGアイコン本来の色を表示するようにしました。
- アイコンの差別化は、画像本来の色と薄い外枠で表現するように調整しました。
- `style.css?v=44` / `app.js?v=44` / 各アイコン画像 `?v=44` にしてブラウザキャッシュを回避しています。


## v45 修正内容

- 左メニューのアイコン差別化を強化しました。
- v44では外枠だけの差別化だったため、見た目の違いが弱かった問題を改善。
- v45では各メニューのアイコン背景そのものに色を付け、視認性を高めています。

配色:
- 今日: イエロー系
- 業務管理: ブルー系
- スケジュール: オレンジ系
- 自分の担当: パープル系
- 完了: グリーン系


## v46 修正内容

- 左メニューのアイコンを新規生成画像に差し替えました。
- 外枠や背景だけではなく、アイコン画像そのものが色付きになるようにしています。
- アイコンサイズが小さく見えていたため、44pxに戻して視認性を上げました。
- 差し替え対象:
  - 今日: `summary-today.png`
  - 業務管理: `nav-board.png`
  - スケジュール: `nav-schedule.png`
  - 自分の担当: `summary-mine.png`
  - 完了: `nav-done.png`
- `style.css?v=46` / `app.js?v=46` / 各アイコン画像 `?v=46` にしてブラウザキャッシュを回避しています。


## v47 修正内容

- 左メニューの機能ボタン名だけを変更しました。
  - 旧：業務管理
  - 新：タスク
- サイト全体名は引き続き「業務管理ボード」のままです。
  - ブラウザタブ
  - 左上タイトル
  - ヘッダー大タイトル
  - 英語表記
  は変更していません。
- これにより、サイト全体は「業務管理ボード」、機能単位は「今日 / タスク / スケジュール」として分かりやすく整理しました。
- `style.css?v=47` / `app.js?v=47` / 各アイコン画像 `?v=47` にしてブラウザキャッシュを回避しています。


## v48 修正内容

- 絞り込み条件解除ボタンを、丸矢印から `C` に変更しました。
- タスク詳細を開いた状態で、クイック追加・検索欄・並び順プルダウンが重なる問題を修正しました。
  - 詳細表示中はツールバーを2段構成にして、項目同士が被らないようにしています。
- スケジュール見出しを改善しました。
  - 日付は `スケジュール` の右側に表示
  - 説明文は下段に分離
- タスクの繰り返し機能を強化しました。
  - 毎日
  - 毎週（曜日指定）
  - 毎月（日付指定）
  - 毎月（第n曜日指定：第1水曜、第3金曜、最終金曜など）
  - 毎年
  - 間隔指定（2週ごと、3か月ごとなど）
- 完了時に、指定した繰り返し条件から次回タスクを自動作成します。
- `style.css?v=48` / `app.js?v=48` / 各アイコン画像 `?v=48` にしてブラウザキャッシュを回避しています。


## v49 修正内容

- v48で残っていた、タスク詳細表示中のツールバー重なりを再修正しました。
- 原因はCSSセレクタの指定方向でした。
  - v48: `.app-shell.detail-open .task-mode .toolbar`
  - 正: `.task-mode .app-shell.detail-open .toolbar`
- `task-mode` は本文側ではなく `body` 側に付くクラスのため、v48の指定が効いていませんでした。
- v49では、詳細表示中のタスク画面でツールバーを確実に2段構成へ切り替えます。
  - 1段目：新しいタスク / クイック追加 / 並び順
  - 2段目：検索欄
- 画面幅がさらに狭い場合は3段構成にして、項目同士が被らないようにしています。
- `style.css?v=49` / `app.js?v=49` / 各アイコン画像 `?v=49` にしてブラウザキャッシュを回避しています。


## v50 修正内容

- 「自分の担当」と「完了」を同時にONにできるようにしました。
  - 例：自分の担当 かつ 完了済み のタスクを表示できます。
- 詳細絞り込みの「状態」で完了を選んだ場合も、上部の「完了」ボタンと同じ扱いになるようにしました。
- 「完了」状態を解除した場合は、完了絞り込みも自動で解除されます。
- 絞り込みのクリアボタン `C` で、詳細絞り込みだけでなく「自分の担当」「完了」のON/OFFもまとめて解除されるようにしました。
- スケジュール画面では「完了」の概念がないため、以下を非表示にしました。
  - 左メニューの完了ボタン
  - 状態・優先度・期限超過のみ・今日のみ・固定のみ
- 今日ビューでも「完了」ボタンを非表示にしました。
- スケジュール画面では、裏側でタスク専用フィルターが効き続けないように、状態・優先度・期限系フィルターを自動解除します。
- `style.css?v=50` / `app.js?v=50` / 各アイコン画像 `?v=50` にしてブラウザキャッシュを回避しています。


## v51 修正内容

- Excel/CSV/タブ区切りテキストからタスクを取り込めるようにしました。
- タスク画面の上部に `Excel取込` ボタンを追加しました。
- 対応形式:
  - `.xlsx`
  - `.xls`
  - `.csv`
  - `.tsv`
  - `.txt`
- 取込ルール:
  - Excelの `割合` が100%の行は `完了`
  - それ以外は `対応中`
  - Excelの `開始日` は作成日として反映
  - Excelの `期日` は期限日として反映
  - Excelの `担当者` は担当者として反映。未登録の担当者は自動追加
  - Excelの `作業内容` と `備考` は内容・メモへ反映
  - Excelの `優先度` 1〜5は、低/中/高/緊急へ変換
    - 5: 緊急
    - 4: 高
    - 3: 中
    - 1〜2: 低
- Excel内容から分類を推定し、必要に応じて分類を自動追加します。
  - 例: 在庫管理、Excel/資料、会議/設営、サーバー/設備、セキュリティなど
- 取込前に件数、完了/対応中、新規分類、新規担当者、重複候補を確認できるプレビュー画面を追加しました。
- 既存タスクとの重複スキップをON/OFFできます。
- `style.css?v=51` / `app.js?v=51` / 各アイコン画像 `?v=51` にしてブラウザキャッシュを回避しています。


## v52 修正内容

- v51を一時的なExcel移行用とし、v52は移行後の通常運用版として作成しました。
- Excel取込機能を削除しました。
  - `Excel取込` ボタンを削除
  - 取込用ファイル選択を削除
  - 取込プレビュー画面を削除
  - Excel読込用の外部ライブラリ SheetJS を削除
  - Excel取込専用のJavaScript処理を削除
  - Excel取込専用CSSを削除
- v51でFirebaseへ取り込んだタスクデータは通常のタスクとして保存されるため、v52でもそのまま表示・編集できます。
- 通常運用時に誤ってExcelを再インポートするリスクをなくしています。
- `style.css?v=52` / `app.js?v=52` / 各アイコン画像 `?v=52` にしてブラウザキャッシュを回避しています。


## v53 修正内容

- 優先度「低」のタスクカード全体が緑色になる問題を修正しました。
- 原因は、優先度バッジ用の `.priority-低` の背景色が、タスクカード本体にも適用されていたことです。
- v53では以下のように整理しました。
  - タスクカード本体：通常の白〜淡青背景
  - 左端の線：優先度の色
  - 優先度バッジ：バッジ内だけ色付き
- 「低」は凡例と同じグレー系に統一しました。
- `style.css?v=53` / `app.js?v=53` / 各アイコン画像 `?v=53` にしてブラウザキャッシュを回避しています。


## v54 修正内容

- 色ルールを全表示で統一しました。
- 優先度「低」が一覧・タイムラインで緑色になる問題を修正しました。
  - 低はグレーに統一しました。
- 優先度は、全体背景ではなく、左端の線とバッジだけで表現するように統一しました。
- 一覧表示とタイムライン表示にも、今日・期限超過・放置・固定の状態クラスを付与しました。
  - これにより、ボード・一覧・タイムラインで同じ色ルールになります。
- 全体背景色は以下の4種類だけに整理しました。
  - 今日: 黄色
  - 期限超過: 赤
  - 放置: 紫
  - 固定: 青緑
- 優先順位は、期限超過 > 今日 > 放置 > 固定 です。
- `style.css?v=54` / `app.js?v=54` / 各アイコン画像 `?v=54` にしてブラウザキャッシュを回避しています。


## v55 修正内容

- ボード画面で、タスクをドラッグ&ドロップして別の状態セクションへ移動できるようにしました。
  - タスクがない空欄部分にもドロップ判定が出るよう、状態セクション全体をドロップ領域にしています。
  - ドロップするとタスクの状態が移動先セクションの状態に更新されます。
- タイムライン画面で、タスクをドラッグ&ドロップして別の日付・別の状態へ移動できるようにしました。
  - ドロップ先セルの状態と日付をタスクに反映します。
  - つまり、タイムライン上で移動すると「状態」と「期限」が同時に更新されます。
- ドラッグ中の視認性を上げるため、移動先候補に点線枠と薄いハイライトを表示します。
- 完了状態へドラッグした場合は、完了日時も反映します。
- `style.css?v=55` / `app.js?v=55` / 各アイコン画像 `?v=55` にしてブラウザキャッシュを回避しています。


## v56 修正内容

- ボードのドラッグ&ドロップ判定を強化しました。
  - タスクの上だけでなく、セクション内の下部空白にもドロップできるようにしました。
  - 各状態セクションに大きめの空白ドロップ領域を追加しました。
  - 他のセクションが画面外にある場合でも、見えている空白部分へ移動しやすくしています。
- タイムラインの期限なしタスクからもドラッグ&ドロップできるように強化しました。
  - 期限なし欄のタスクを日付セルへ移動すると、状態と期限が設定されます。
  - 日付セルのタスクを期限なし欄へ戻すこともできます。
- ドラッグ処理をイベント委任方式に変更し、期限なし欄を含むタイムライン全体で安定して動くようにしました。
- 集計カードの「未完了」「期限超過」アイコンを新規生成し、「今日」「自分の担当」とサイズ感を合わせました。
- `style.css?v=56` / `app.js?v=56` / 各アイコン画像 `?v=56` にしてブラウザキャッシュを回避しています。


## v57 修正内容

- ボードの移動用ドロップ枠を、通常時は非表示にしました。
  - ドラッグ中だけ「ここへ移動」の枠が表示されます。
  - 空欄部分へドロップできる機能は維持しています。
- セクション追加がページ中央付近に下がっていた問題を修正しました。
  - セクション追加は常に列の上部に表示されます。
- タイムラインの期限なしタスクを日付セルへ移動できない問題を再修正しました。
  - DataTransferだけに依存せず、ドラッグ中のタスクIDを内部状態でも保持するようにしました。
  - これにより、期限なし欄から日付セルへのドラッグ&ドロップが安定します。
- `style.css?v=57` / `app.js?v=57` / 各アイコン画像 `?v=57` にしてブラウザキャッシュを回避しています。


## v58 修正内容

- ボードでタスク移動後に、移動用の点線枠が表示されたままになる問題を修正しました。
- drop後、dragend後、画面外drop後のすべてでドラッグ状態を強制解除する処理を追加しました。
- 期限なしタスクからタイムライン上へ移動できない問題を再修正しました。
- 期限なし欄のタスクも、カード単位でdragstartイベントを確実に付与する方式へ変更しました。
- タイムライン上で移動した後に「ここへ移動」が消えない問題も同時に修正しました。
- セクション追加が下がらないよう、ドラッグ中でも上部表示を維持します。
- `style.css?v=58` / `app.js?v=58` / 各アイコン画像 `?v=58` にしてブラウザキャッシュを回避しています。


## v59 修正内容

- タイムラインの期限なしタスクがドラッグ開始できない問題を再修正しました。
- 原因候補として、期限なし欄のタスクが `button` 要素だったことで、ブラウザ側のドラッグ開始判定が安定しない可能性が高かったため、タイムライン用タスクを `article role="button"` に変更しました。
- 見た目とクリック・ダブルクリック動作は維持しています。
- ドラッグ時に掴んでいるカードが見えやすいよう、ドラッグ画像も明示的に設定しました。
- `style.css?v=59` / `app.js?v=59` / 各アイコン画像 `?v=59` にしてブラウザキャッシュを回避しています。


## v60 修正内容

- 期限なしタスクに `タイムラインへ` ボタンを追加しました。
  - タイムライン下部の期限なし欄にだけ表示されます。
  - 押すと、状態と期限日を選択する小さな画面が開きます。
  - 保存すると、そのタスクがタイムライン上に表示されます。
- ドラッグが不安定だった期限なしタスクについては、無理にドラッグ操作へ寄せず、安定して移動できるボタン方式を追加しました。
- サイト左メニュー下部に `Ver.60` を小さく表示しました。
  - 今後、更新できているか確認しやすくなります。
- `style.css?v=60` / `app.js?v=60` / 各アイコン画像 `?v=60` にしてブラウザキャッシュを回避しています。


## v61 修正内容

- タイムライン下部の `期限なし` タスクの件名表示を統一しました。
- これまでは短い件名だけ、担当者・優先度バッジと同じ1行目に表示されていました。
- v61では、短い件名でも長い件名でも、件名は必ず2行目に表示されます。
- 長い件名は最大2行で省略表示します。
- `style.css?v=61` / `app.js?v=61` / 各アイコン画像 `?v=61` にしてブラウザキャッシュを回避しています。


## v62 修正内容

- タイムライン下部の `期限なし` タスクに表示される移動ボタンを小型化しました。
- ボタン位置をカード右上に固定しました。
- 表示文言を `タイムラインへ` から `移動` に短縮し、主張を弱めました。
- 件名表示を邪魔しないように、件名エリアを広く使える配置へ調整しました。
- `style.css?v=62` / `app.js?v=62` / 各アイコン画像 `?v=62` にしてブラウザキャッシュを回避しています。


## v63 修正内容

- スケジュールの予定入力・編集画面が横に広がりすぎ、横スクロールが出る問題を修正しました。
- スケジュールダイアログの横幅を画面内に収まるよう制限しました。
- 入力欄・プルダウン・メモ欄がダイアログ幅を押し広げないように調整しました。
- 小さい画面では2列フォームを1列に切り替え、横スクロールが出ないようにしました。
- `style.css?v=63` / `app.js?v=63` / 各アイコン画像 `?v=63` にしてブラウザキャッシュを回避しています。


## v64 修正内容

- 今日ビューに `お知らせ` パネルを追加しました。
  - 他の担当者が追加した新規タスク・新規予定を表示します。
  - 重要な更新だけを優先表示します。
  - 重要更新の対象例:
    - 自分の担当
    - 今日・明日の予定
    - 今日/明日まで、または期限超過のタスク
    - 優先度が高/緊急のタスク
  - 重要度が低い更新は件数だけ表示し、画面が情報過多にならないようにしています。
- `確認済みにする` ボタンを追加しました。
  - 押した時点以降の新規・重要更新だけがお知らせに出ます。
- スケジュールの15分前通知を追加しました。
  - 今日ビューの `予定通知ON` ボタンからブラウザ通知を許可できます。
  - 通知は1予定につき1回だけです。
  - 通知対象は、自分担当の予定、または担当者が `システム` / `全員` / `共通` の予定です。
- 通知時は、ブラウザ通知・画面上の通知・タブタイトルの一時変更を行います。
- ブラウザ通知は、業務管理ボードのページをブラウザで開いている場合に動作します。
- `style.css?v=64` / `app.js?v=64` / 各アイコン画像 `?v=64` にしてブラウザキャッシュを回避しています。


## v65 修正内容

- 今日ビュー上部の以下を非表示にしました。
  - 未完了 / 期限超過 / 今日 / 自分の担当 の集計カード
  - 新しいタスク / クイック追加 の上部レコード
  - 表示形式・統計・色の意味のタスク操作レコード
- 今日ビューでは、表示順を以下に変更しました。
  - お知らせ
  - 今日やること
  - 今日の予定 / 期限超過 / 今日までのタスク など
- ヘッダーの表示内容を画面ごとに変更しました。
  - 今日: 本日の業務確認
  - タスク: タスク進行管理
  - ダッシュボード: 進捗ダッシュボード
  - スケジュール: 予定・スケジュール管理
- 共通で同じ「業務管理ボード」が表示され続ける状態を改善しました。
- ヘッダーは削除せず、画面の雰囲気を保つために控えめなビジュアル見出しとして残しています。
- `style.css?v=65` / `app.js?v=65` / 各アイコン画像 `?v=65` にしてブラウザキャッシュを回避しています。


## v66 修正内容

- お知らせのタイトルUIを変更しました。
  - 丸みのある見出し、アイコン、件数バッジを追加し、やわらかい見た目にしました。
- お知らせの「重要更新」を、更新内容が分かる表示へ変更しました。
  - 期限変更
  - 担当変更
  - 状態変更
  - 優先度変更
  - 件名変更
  - 内容更新
  - コメント追加
  - 日時変更
  - 場所変更
  など、何が変わったかをラベルで表示します。
- 通知カード内に変更内容の要約を表示するようにしました。
  - 例: `期限: 2026-07-02 → 2026-07-04`
  - 例: `担当: 土屋 → 福冨`
  - 例: `作業メモが追加されました`
- 今後のタスク・予定保存時に、変更内容を `lastChange` として保存するようにしました。
- `style.css?v=66` / `app.js?v=66` / 各アイコン画像 `?v=66` にしてブラウザキャッシュを回避しています。


## v67 修正内容

- お知らせタイトルUIを再調整しました。
- `✦` の単独アイコンを廃止しました。
- 見出しは `ベル風アイコン + お知らせ` を1つのまとまりにし、通知件数は別の小さな件数バッジとして分けました。
- アイコン、タイトル、件数がすべて同じ形・同じ色に見えていた問題を修正しました。
- `style.css?v=67` / `app.js?v=67` / 各アイコン画像 `?v=67` にしてブラウザキャッシュを回避しています。


## v68
- お知らせタイトルUIを再デザイン
- 全体の青系・やわらかいガラス感のあるデザインに寄せて調整
- 謎の単独マーク感をなくし、ベル風アイコン＋お知らせのまとまりに整理
- 通知件数は補助的な小バッジに変更


## v69 修正内容

- お知らせタイトルUIをD案ベースのシンプル見出し型に変更しました。
- ベルアイコンを赤ピンク系に変更しました。
- 文字化けや謎マークに見える装飾は使わず、CSSでベル風アイコンを作成しています。
- 件数バッジは補助的に控えめな赤系バッジとして表示します。
- `style.css?v=69` / `app.js?v=69` / 各アイコン画像 `?v=69` にしてブラウザキャッシュを回避しています。


## v70-fixed 修正内容

- お知らせタイトルのベルを CSS自作アイコン から `🔔` 絵文字に変更しました。
- ベル背景は赤ピンク系の柔らかい色に調整しました。
- `お知らせ` 文字の枠を削除し、通常の見出し文字として表示するようにしました。
- 旧CSSのベル自作パーツを無効化しました。
- `style.css?v=70` / `app.js?v=70` / 各アイコン画像 `?v=70` にしてブラウザキャッシュを回避しています。


## v71-fixed 修正内容

- お知らせタイトルの `🔔` の背景を削除しました。
- ベルは絵文字だけで表示されるようにしました。
- ベル周りの枠線・影・角丸背景をすべて無効化しました。
- `style.css?v=71` / `app.js?v=71` / 各アイコン画像 `?v=71` にしてブラウザキャッシュを回避しています。


## v72-fixed 修正内容

- お知らせの新規タスク・新規予定で、件名下に同じ内容が二重表示される問題を修正しました。
  - 新規追加時は、変更内容行を省略し、基本情報だけを表示します。
  - 更新時は、これまで通り「期限変更」「担当変更」「優先度変更」などの変更内容を表示します。
- お知らせの基本情報に項目名を追加しました。
  - タスク: `担当: システム / 対応中 / 優先度: 高 / 期限なし`
  - スケジュール: `2026-07-07 16:00〜17:00 / 担当: 福冨`
- 共有ルーム名を、ユーザーとは別の「共有の担当先」として選べるようにしました。
  - タスク・予定の担当者プルダウンに `共有ルーム：〇〇` が表示されます。
  - 保存される担当名は共有ルーム名です。
  - 共有ルーム担当のタスク・予定は、各ユーザーの「自分の担当」に含まれます。
  - 共有ルーム担当のタスク・予定は、各ユーザーのお知らせ・予定通知対象になります。
- 共有ルーム担当のバッジは、通常ユーザーと区別できる水色系にしました。
- `style.css?v=72` / `app.js?v=72` / 各アイコン画像 `?v=72` にしてブラウザキャッシュを回避しています。


## v73-fixed 修正内容

- 一覧画面の一括変更で、担当者欄に共有ルーム担当が表示されない問題を修正しました。
  - 一括変更の担当者候補にも `共有ルーム：システム課` のように表示されます。
  - 選択して適用すると、担当者は共有ルーム名として保存されます。
- 一括変更時にも、変更内容を `lastChange` に保存するよう改善しました。
  - お知らせ欄で `担当変更` などの変更内容が追いやすくなります。
- `style.css?v=73` / `app.js?v=73` / 各アイコン画像 `?v=73` にしてブラウザキャッシュを回避しています。


## v74-fixed 修正内容

- 今日ビューのお知らせ表示を、最大10件までに変更しました。
- 10件を超える重要なお知らせがある場合は、`すべて見る` ボタンを表示します。
- `すべて見る` から、お知らせ一覧ダイアログを開けるようにしました。
  - 重要なお知らせ
  - その他の更新
  に分けて表示します。
- 一覧ダイアログ内のお知らせカードをクリックすると、対象のタスクまたは予定を開けます。
- `style.css?v=74` / `app.js?v=74` / 各アイコン画像 `?v=74` にしてブラウザキャッシュを回避しています。


## v75-fixed 修正内容

- ボードでタスクをドラッグして他の状態セクションへ移動する際、移動先の状態名を画面中央に大きく表示するようにしました。
- セクションが縦に長く、移動先の見出しが画面外にある場合でも、どの状態へ移動しようとしているか分かるようになりました。
- ドロップ判定はこれまで通り、セクション全体で受けられる仕様を維持しています。
- `style.css?v=75` / `app.js?v=75` / 各アイコン画像 `?v=75` にしてブラウザキャッシュを回避しています。


## v76-fixed 修正内容

- ユーザー別スター機能を追加しました。
  - 各タスクに `☆ / ★` ボタンを追加しました。
  - スターは現在のユーザーごとに保存されます。
  - 他ユーザーのスター状態とは分かれます。
- スター操作に対応した画面:
  - ボード
  - 一覧
  - タイムライン
  - タスク詳細
- 左メニューの絞り込みに `スター` を追加しました。
  - 押すとスター付きタスクだけを表示できます。
- 詳細絞り込みにも `スターのみ` を追加しました。
- 保存済みフィルターにもスター条件を含められるようにしました。
- `style.css?v=76` / `app.js?v=76` / 各アイコン画像 `?v=76` にしてブラウザキャッシュを回避しています。


## v77-fixed 修正内容

- タイムライン上のスターボタンを削除しました。
  - タイムラインは表示スペースが狭いため、スター操作はタスク詳細から行う仕様に変更しました。
  - ボード・一覧・タスク詳細のスター機能はそのまま残しています。
- タイムラインで優先度バッジが横長になっていた表示崩れを修正しました。
- スター機能の追加により不要になったナレッジ機能のUIを削除しました。
  - タスク詳細の `ナレッジ化` / `ナレッジ解除` ボタンを削除
  - タスク詳細内の `ナレッジ` 表示欄を削除
  - ダッシュボードの `最近のナレッジ` パネルを削除
- 既存の内部データは安全のため削除せず、画面上の機能として非表示・廃止しています。
- `style.css?v=77` / `app.js?v=77` / 各アイコン画像 `?v=77` にしてブラウザキャッシュを回避しています。


## v78-fixed 修正内容

- 左メニューの `スター` 絞り込みアイコンを改善しました。
- これまで背景と同化して見えづらかったため、金色系の背景に変更しました。
- 他の絞り込みアイコンとサイズ感を合わせつつ、スターだけ視認性が出るように調整しました。
- `style.css?v=78` / `app.js?v=78` / 各アイコン画像 `?v=78` にしてブラウザキャッシュを回避しています。


## v79-fixed 修正内容

- 今日ビューの左メニューアイコン色を薄いターコイズ系に変更
- 今日ビューのアイコンを「カレンダー＋チェック」に変更
- スター絞り込みの選択時カラーを、より黄色みの強い色へ変更
- スケジュールの左メニューアイコンを「リスト＋時計」に変更
- バージョン表記を `Ver.79` に更新


## v80-fixed 修正内容

- 今日ビューの左メニューアイコンを、スター選択時に近いターコイズ系に再調整
- 今日ビューのアイコンを新ファイル化し、「カレンダー＋チェック」に確実に変更
- スケジュールアイコンを新ファイル化し、「リスト＋時計」に確実に変更
- スケジュールアイコンの色面が消えないよう背景色を明示
- スター選択時の色味を、他メニューと区別しやすい黄み強めに再調整
- バージョン表記を `Ver.80` に更新


## v81 更新内容
- 左メニュー「今日」ビューのアイコンを新規生成し、「カレンダー＋チェック」へ変更
- バージョン表記を Ver.81 に更新

## v82 更新内容
- 左メニュー「スケジュール」のアイコンを新規生成し、「カレンダー＋時計」へ変更
- 左メニュー「今日」のアイコンを、白アイコン＋ターコイズ背景の2色構成に整理
- 左メニュー「スケジュール」のアイコンを、白アイコン＋コーラル背景の2色構成に整理
- バージョン表記を `Ver.82` に更新
- `style.css?v=82` と各メニューアイコンの `?v=82` 指定でブラウザキャッシュを回避

## v83 更新内容
- 左メニュー「タスク」のアイコンを新規生成し、白アイコン＋ブルー背景のチェックリストアイコンへ変更
- 左メニュー「今日」「スケジュール」のアイコン画像内余白を減らし、他の左メニューアイコンとサイズ感を合わせました
- 左メニュー「スター」のホバー色と選択中の色を整理し、未選択時ホバーは明るく、選択中は濃い黄色グラデーションになるように修正
- バージョン表記を `Ver.83` に更新
- `style.css?v=83` と各メニューアイコンの `?v=83` 指定でブラウザキャッシュを回避

## v84 更新内容
- 左メニュー「今日」「タスク」「スケジュール」のアイコンを、段差のある2トーン背景と薄い白枠で3D風に調整
- 左メニュー「スター」アイコンも、段差のある黄色2トーン背景と薄い白枠で3D風に調整
- 選択中のアイコンでも白い外枠が見えるよう、アイコン単体の立体感を強化
- バージョン表記を `Ver.84` に更新
- `style.css?v=84` と各メニューアイコンの `?v=84` 指定でブラウザキャッシュを回避


## v85 更新内容

- `app.js?v=81` のままだったキャッシュ指定漏れを `app.js?v=85` に更新しました。
- 左メニューの `今日・タスク・スケジュール・スター` アイコンをすべて新しい v85 PNG に差し替えました。
- スターはテキストアイコンではなく画像アイコン化し、他のアイコンと同じ更新方式に統一しました。
- CSS末尾に最終上書きルールを追加し、古い `.nav-link` や `.nav-star-icon` 系の残存CSSに影響されないようにしました。
- `index.html` にキャッシュ抑制用 meta を追加しました。
