# Ver.149 前提タスク制御とコメント導線の改善

## 目的
Ver.148で追加したタスク依存関係を、単なる注意表示ではなく実際の業務順序を守れる機能へ整理する。また、利用頻度が高いコメント入力をタスク詳細下端までスクロールせず利用できるようにする。

## 前提タスク
- UI上の「ブロック中」という表現を「前提タスク未完了」へ変更する。
- 未完了の前提タスクが1件以上存在する間は、対象タスクを「完了」へ変更できない。
- 完了以外の状態変更、内容編集、コメント、チェックリスト等は通常どおり利用できる。
- 完了を試みた場合は、未完了の前提タスクがあることと、先に完了するか関係を解除する必要があることをメッセージ表示する。
- 詳細の完了ボタン、クイック状態変更、編集フォーム、ボード／タイムラインのドラッグ、一括完了、タイムライン移動からの完了を対象とする。
- 循環する依存関係は新規設定できない。
- 削除済みの前提タスクは完了制限の対象外とし、関係の解除を促す。

## コメントUIの調査と判断
- ClickUp 4.0はタスク右サイドバーにComments専用ページを設け、コメント・アクティビティへ素早く移動できる。
- JiraはActivity内をComments / History等で切り替えられる。
- Asanaはコメントが詳細下部にある一方、キーボードでコメント欄へ移動できる導線を持つ。
- Linearもコメント投稿を軽量な主要操作として扱う。

当サイトはすでに右側がタスク詳細パネルのため、別の右サイドバーを増設せず、タイトル・状態・操作ボタンを常時表示したまま、その下を「詳細 / コメント / 履歴」の3タブへ分割する方式を採用する。

## コメントタブ仕様
- 「コメント」タブを選ぶと、最上部にコメント入力欄を表示する。
- 既存のコメント種別、本文、保存処理、Firebase競合制御は変更しない。
- コメント投稿でタスク詳細が再描画されても、同じタスクではコメントタブを維持する。
- 「履歴」は別タブとして分離し、コメントと履歴を混在させない。
- PC・スマホとも同じ3タブ構成とする。
