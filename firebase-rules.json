{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": true,
        ".write": true,
        "tasks": {
          ".indexOn": [
            "status",
            "assignee",
            "priority",
            "dueDate",
            "updatedAt"
          ]
        }
      }
    }
  }
}