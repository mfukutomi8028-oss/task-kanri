# Ver.150 業務管理機能

## 完了時の固定解除
- 完了状態のタスクは固定状態を維持しません。
- 完了処理後に `pinned=true` が残っている場合は自動的に解除します。
- 既に完了済みなのに固定が残っている既存データもVer.150読み込み後に整理します。
- 固定解除では完了時刻・更新日時を上書きせず、完了一覧の基本並び順へ不要な影響を与えません。

## 関連タスク
- 前提関係ではないものの、同じ案件・同じ機器・同じ対応テーマなど、一緒に確認したいタスクを相互に関連付けられます。
- 関連付けは前提タスクとは別機能で、完了制御には影響しません。
- 片側で関連付け・解除すると反対側にも反映します。
- タスク詳細から関連タスクを直接開けます。

## フォローアップタスク
- タスク詳細の関連タスク欄から「フォローアップ作成」を実行できます。
- 新規タスク画面を開き、元タスク名を基に件名とメモを事前入力します。
- 保存が成功した場合、元タスクと新しいフォローアップタスクを自動で「関連タスク」として結びます。

## 個人確認リマインダー
- タスク期限とは別に、自分だけの再確認日時と短いメモを設定できます。
- 当日分と期限を過ぎたリマインダーは今日ビューに表示します。
- 時刻到達後は画面内通知を表示し、ブラウザ通知が既に許可済みの場合だけOS通知も利用します。通知許可を新たに要求しません。
- タスクが完了した場合は、そのユーザーの不要になったリマインダーを自動解除します。
- リマインダーは既存 `rooms/{room}/workflowV148` の補助領域へ保存し、タスク本体のデータ構造を変更しません。
