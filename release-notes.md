# Ver.142 release notes

- ToDoアイコンの実描画領域を画像上で比較し、他メニュー44pxに対して約38pxだった差を修正。
- `nav-todo-v142.svg` のviewBoxを実絵の108px範囲へ合わせ、SVG内部余白を原因から解消。
- CSSのscale補正を廃止し、他メニューと同じ44pxの実描画サイズへ統一。
- ToDo固有の選択時リングを整理し、他の主要メニューと同じ自然なサイズ感・影に統一。

# Ver.141 release notes

- ToDo左メニューアイコンを44px規格へ統一し、SVG内部余白を補正。
- 未選択時にもミント系の背景面を表示し、選択時だけ余白が目立つ状態を改善。
- ToDoタイトル行と詳細・メモ編集領域にわずかな背景差を追加し、開閉対象を認識しやすく改善。
- 展開中タイトル行へ左アクセントを追加。

# Ver.140 release notes

- ToDo詳細編集を横2列から「やること → メモ」の縦並びへ変更。
- タイトル入力欄を1行分の高さへ固定し、メモ・保存領域を含め不要な余白を削減。
- 上下移動・タスク化・削除のボタン高を36pxへ統一。
- ToDo通常行もさらにコンパクト化し、一覧性を改善。
- 未完了サマリーカードのアイコンを `summary-open.png` へ復旧。

# Ver.139 release notes

- 今日ビューのToDoをコンパクトなプレビューへ縮小し、メインの確認導線を阻害しないように改善。
- 左メニューに「ToDo」を追加し、ToDo専用ページで追加・メモ編集・並び替え・タスク化をまとめて操作できるよう改善。
- ToDo追加フォーム、詳細メモレイアウト、各カードの余白・配置を再整理し、見やすさを改善。
- ToDo一覧の入力欄・追加ボタンの幅崩れを修正。

- ToDoに任意メモを追加し、各ToDoを開いてタイトル・メモを確認・編集できるようにしました。既存のメモなしToDoはそのまま互換表示します。
- ToDoのタスク化時に、タイトルをタスク件名、メモをタスクの「内容・メモ」へコピーします。タスク保存成功後にのみ従来どおり元ToDoを完了扱いにします。
- ToDoをカード型UIへ整理し、「タスク化」をアイコン付きの専用アクションへ変更しました。並び替えと削除も含め、長文とスマホ幅で崩れにくい配置へ改善しました。
- 未整理タスクのクイック追加は「未整理タスクをクイック追加」「クイック追加」へ文言を簡潔化し、旧62/64px固定幅によるボタン文字のはみ出しを解消しました。

認証・Firebase Rules・本番接続は変更していません。

---

# Ver.136 release notes

- 今日画面に軽量な「今日のやること」を追加しました。正式タスクとは別の `rooms/{room}/todos/{id}` に保存します。
- 追加、完了切替、上下移動、削除、既存タスクダイアログへのタスク化を提供します。タスク化後のToDo完了はタスク保存成功後だけです。
- 既存クイック追加は「未整理の正式タスク」として維持し、ラベルを明確化しました。
- ToDoのFirebase map keyを操作IDの正本とし、遅延した購読結果では削除済みToDoを復活させず、対象ToDoだけを有限に同期します。上下移動は画面に見えている同じ一覧内で行います。
- ToDo保存中は入力欄または対象行を即時に操作不可にし、狭い画面では本文と操作群を二段に分けます。予定起点とToDo起点のタスク作成コンテキストも混在しません。

認証・Firebase Rules・本番接続は変更していません。ToDoのユーザー分離は表示上のもので、秘匿を保証しません。