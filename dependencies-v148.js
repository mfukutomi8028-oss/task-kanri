// Ver.148 task dependency/blocking UI.
(function(){const W=window.WorkBoardWorkflowV148;if(!W)return;let scheduled=false;const esc=v=>String(v||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'})[c]);
function detailId(d){const k=d?.querySelector?.('[data-action="delete"]')?.dataset?.operationKey||'';return k.startsWith('task-delete:')?k.slice(12):''}
function patchDetail(map){const detail=document.getElementById('detailBody');if(!detail||detail.classList.contains('empty'))return;const task=map.get(detailId(detail));if(!task)return;const ids=W.depIds(task.id),active=W.activeBlockers(task.id,map),sig=`${task.id}|${ids.join(',')}|${ids.map(id=>map.get(id)?.revision||0).join(',')}`;let section=detail.querySelector('.workflow-dependencies-v148');if(section?.dataset.signature===sig)return;if(section)section.remove();const candidates=[...map.values()].filter(x=>x&&x.id!==task.id&&!W.isCompleted(x)&&!ids.includes(String(x.id))).sort((a,b)=>String(a.title||'').localeCompare(String(b.title||''),'ja'));section=document.createElement('section');section.className='detail-section workflow-dependencies-v148';section.dataset.signature=sig;section.innerHTML=`<div class="workflow-dependency-head-v148"><div><h4>依存・ブロック</h4><p>${active.length?`未完了の前提タスクが${active.length}件あります。`:ids.length?'前提タスクはすべて完了しています。着手できます。':'他のタスク待ちを設定できます。'}</p></div>${active.length?'<span class="workflow-blocked-badge-v148">⛔ ブロック中</span>':ids.length?'<span class="workflow-ready-badge-v148">✓ ブロック解除</span>':''}</div>${ids.length?`<ul class="workflow-dependency-list-v148">${ids.map(id=>{const b=map.get(id),done=!b||W.isCompleted(b);return`<li><span class="workflow-dependency-state-v148 ${done?'is-done':'is-blocking'}">${done?'✓ 完了':'⛔ 待ち'}</span><span class="workflow-dependency-title-v148">${esc(b?.title||'削除済みのタスク')}</span><button type="button" class="ghost-button" data-remove-dependency-v148="${esc(id)}">解除</button></li>`}).join('')}</ul>`:''}<div class="workflow-dependency-add-v148"><select data-dependency-select-v148><option value="">待っているタスクを選択</option>${candidates.map(x=>`<option value="${esc(x.id)}">${esc(x.title||'名称未設定')}</option>`).join('')}</select><button type="button" class="ghost-button" data-add-dependency-v148 ${candidates.length?'':'disabled'}>＋ 待ちタスクを追加</button></div><p class="workflow-dependency-help-v148">前提タスクが完了すると、自動で「ブロック解除」に切り替わります。タスクの状態自体は勝手に変更しません。</p>`;const first=detail.querySelector('.detail-section');first?first.insertAdjacentElement('afterend',section):detail.appendChild(section);section.querySelector('[data-add-dependency-v148]')?.addEventListener('click',async()=>{const id=section.querySelector('[data-dependency-select-v148]')?.value||'';if(!id)return;const r=await W.writeDependencies(task.id,[...ids,id]);if(r.ok)W.notify('待ちタスクを追加しました。')});section.querySelectorAll('[data-remove-dependency-v148]').forEach(b=>b.addEventListener('click',async()=>{const id=b.dataset.removeDependencyV148||'';const r=await W.writeDependencies(task.id,ids.filter(x=>x!==id));if(r.ok)W.notify('依存関係を解除しました。')}))}
function patchSurfaces(map){document.querySelectorAll('.task-card[data-task-id],tr[data-task-id],.timeline-task[data-task-id],.attention-item[data-task-id]').forEach(node=>{const task=map.get(String(node.dataset.taskId||''));if(!task)return;const active=W.activeBlockers(task.id,map),sig=`${task.revision||0}|${active.map(x=>x.id).join(',')}`;if(node.dataset.depV148===sig)return;node.dataset.depV148=sig;node.querySelectorAll('.workflow-blocked-inline-v148').forEach(x=>x.remove());node.classList.toggle('workflow-is-blocked-v148',active.length>0);if(active.length){const badge=document.createElement('span');badge.className='badge workflow-blocked-inline-v148';badge.textContent=`⛔ ブロック中 ${active.length}`;(node.querySelector('.task-meta,.timeline-task-line')||node.cells?.[2]||node.querySelector('span')||node).appendChild(badge)}})}
function patch(){const map=W.taskMap();patchDetail(map);patchSurfaces(map)}function schedule(){if(scheduled)return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;patch()})}window.addEventListener('workflow-v148-update',schedule);new MutationObserver(m=>{if(m.some(x=>x.addedNodes.length||x.removedNodes.length))schedule()}).observe(document.getElementById('mainContent')||document.body,{childList:true,subtree:true});setInterval(schedule,60000);patch();})();
