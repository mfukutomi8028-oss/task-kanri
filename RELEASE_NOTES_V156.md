# Ver.156 リリースノート

## コメントのメンションUIを整理
- コメント欄にユーザーごとのメンションボタンを並べる方式を廃止し、「メンション」ボタン1つに集約しました。
- 「メンション」を押すと、タスク編集画面に近い中央ダイアログでユーザー一覧を表示します。
- ユーザー名の検索に対応しました。
- 複数ユーザーを選択して、まとめて `@ユーザー名` をコメント入力欄へ挿入できます。
- 登録ユーザーが5人、10人と増えてもコメント入力欄が縦に長くならない構成です。
- 自分自身は従来どおりメンション候補から除外します。

## 掲載済みコメントを選択・コピーできるよう修正
- コメントタブ表示中に画面更新処理が走ると、コメント入力欄へプログラム的にフォーカスが戻ることがあり、掲載済みコメントの文字選択が解除されていました。
- Ver.156ではコメント入力欄への不要なプログラムフォーカスを抑止しました。
- 掲載済みコメント本文に `user-select: text` を明示し、マウスドラッグでの選択・コピーを優先します。
- メンション挿入後など、入力欄へ戻す必要がある操作だけは明示的にフォーカスを戻します。

## 互換性
- コメントデータ、@メンションの文字列形式、通知判定ロジックは変更していません。
- 既存コメントやユーザー情報のデータ移行は不要です。
