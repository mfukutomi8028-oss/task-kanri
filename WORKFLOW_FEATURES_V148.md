# Ver.148 業務管理機能の採否と実装方針

## 今回見送った機能

### 依頼受付・トリアージ
当院では電話・口頭依頼が中心で、最終的にシステム課側が業務管理ボードへ入力する運用になるため、専用の受付箱を追加すると既存の「未整理タスクをクイック追加」と役割が重複します。入力工程を増やさないことを優先し、今回は追加しません。

### 依頼フォーム
他部署職員によるフォーム入力を前提とするため、PC操作が苦手な利用者が多い現状では普及コストが高く、電話・口頭依頼を置き換える効果が期待しづらいため追加しません。

## 実装した機能

### 1. タスク依存・ブロック
- タスク詳細から「待っているタスク」を追加・解除できます。
- 未完了の前提タスクがある場合は自動で「ブロック中」と表示します。
- 前提タスクが完了した場合は、元タスクの状態を勝手に変更せず「ブロック解除・着手可能」と判定します。
- 依存情報は既存タスク本体とは分離して `rooms/{room}/workflowV148/dependencies` に保存します。

### 2. 保存ビュー強化
- 既存の絞り込み条件・基本並び順に加えて、ボード／一覧／タイムラインの表示形式を保存します。
- 一覧表示の場合は列見出しで指定した追加並び順も保存します。
- 既存の保存フィルター形式を壊さず、追加情報だけを `workflowV148/savedViews` に保持します。

### 3. 対応時間・滞留時間
- タスク詳細に作成からの経過時間、最終更新からの経過時間、期限までの残り時間または超過時間を表示します。
- 7日以上更新がない未完了タスクは「更新なし○日」のように具体的な時間で表示します。

### 4. 軽量自動化（自動アシスト）
- 状態・優先度・担当者を自動変更するルールは実装しません。
- 期限超過、7日以上未更新、依存先完了により着手可能になったタスクを自動抽出します。
- 自動判定は通知・可視化に限定し、業務判断は利用者に残します。

### 5. 担当負荷
- 未完了件数だけでなく、優先度、期限超過・期限接近、長期未更新、ブロック状況を加味した簡易負荷指数を表示します。
- 「低・中・高」は人事評価や実工数ではなく、タスク管理上の注意度を示す目安です。
